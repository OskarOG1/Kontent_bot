# Rozwój projektu: dziennik na bieżąco

Ten plik uzupełniamy w trakcie pracy, nie na końcu. Wpis dopisujemy po każdym zadaniu, decyzji, odkryciu albo nowym wymaganiu od właściciela. Najnowsze wpisy na górze sekcji „Dziennik". Plany części: `PLAN_EDITY_0_MAPA.md` i kolejne. Ten plik nie zastępuje planów, tylko zapisuje to, czego w planach nie ma: co faktycznie się stało, co odkryliśmy i czego właściciel zażądał po drodze.

## Stan na 2026-09-23

| Część | Status |
|---|---|
| 1 szkielet | kod gotowy, testy 33 z 33, pomiar w progach. Odbiór 2026-09-22 (Opus): OK. Test ręczny na Telegramie: materiały działają, wzór nie (limit 20 MB, patrz „Znane problemy") |
| 2 analiza | kod gotowy, scalony do `main` w PR #1 (`4b929c9`), testy 62 z 62, pomiar A w progach (100% cięć, błąd tempa maks 0,35%), pomiar B na dwóch prawdziwych wzorach. Odbiór 2026-09-22 (Opus): OK, domyślny ContentDetector potwierdzony. Brak testu ręcznego na prawdziwym wzorze (czeka na lokalny serwer Bot API) |
| 7 wdrożenie | 7.1 i 7.2 scalone do `main` w PR #2 (`e9a4755`), na `46.62.151.181` działa bot testowy, testy w kontenerze 71 z 71. 7.3 wykonane na gałęzi `wdrozenie-poprawki` (2026-09-23), testy lokalnie 75 z 75, pomiar w progach. Odbiór 7.3 (Opus) 2026-09-23: OK, scalone w PR #3. `Pomiary/` zostaje w repozytorium (decyzja właściciela 2026-09-23). Serwer przełączony na głównego bota `@cwel54_bot`, punkty a do f i h zrobione 2026-09-23: pobieranie przez serwer lokalny odblokowane, limit pamięci 3 GB działa, cron ustawiony. Punkt g zrobiony 2026-09-23: wzór 153 MB pobrany w 9 s, analiza 51 s, szczyt pamięci 894 MB przy limicie 3 GB, wolumen serwera Bot API po skopiowaniu pusty. Część 7 zamknięta |
| 3 render | scalona w PR #4, testy 116 z 116, pomiar A i B w progach (100% granic, mediana 8 ms; B 36,5 MB). Pomiar C tylko ręcznie zweryfikowany kodem, bez arkuszy PNG. Odbiór 2026-09-23 (Opus): OK. Test ręczny 2026-09-23: montaż odrzucony przez weryfikację długości (dźwięk ucięty przez `atrim`+`-shortest`), naprawa w zadaniu 3.7 (przycinanie opcjami wejścia, wymuszona długość wyjścia), wdrożona na serwer, powtórzona na projekcie `20260923_163553`: 32,0 s obraz i dźwięk zgodnie. Właściciel potwierdził wynik. Część 3 zamknięta, naprawa scalona w PR #5 |
| 4 muzyka | kod gotowy na gałęzi `muzyka`, 2026-09-23. Testy 144 z 144, pomiar A/B/C w progach, arkusze D powstały. Tryb `dzwiek_wzoru` wyłączony decyzją właściciela (dziennik zadania 4.2 i 4.4): `wybierz_utwor` zawsze idzie po tempie. Scalona w PR #7 (`dc46f0b`) bez odbioru oceniającego. Zadanie 4.5 (pamięć analizy utworu) wykonane 2026-09-24 na gałęzi `pamiec`: testy 189 z 189, pomiar A i B w progach (szczyt analizy najdłuższego utworu 727,9 MB, szczyt renderu 737,5 MB). Bez wdrożenia i bez odbioru oceniającego |
| 8 nakładka | scalona w PR #9 i #10 (2026-09-24), wdrożona. Testy 187 z 187 (190 s). Odbiór ponowny 2026-09-24 (Opus): OK. Pomiar A 5 z 5 wzorów w granicy 0,5 s (maks 0,1 s), arkusze C poprawne (hak czysty, nakładka od dropu, plansza bez nakładki i bez znaku, znak wodny jak w `0914`). Pomiar B niewiarygodny (narzut ujemny), zasada czasu procesora dopisana do bloku WSPÓLNE. Test ręczny: kod -9 przez brak pamięci w analizie utworów z części 4, nie przez nakładkę (znany problem 9) |
| 5, 6, 9 | plany przepisane 2026-09-23 po przeglądzie wzorów (9 nowy), kod nie ruszony. Kolejność po części 8: 5, 6, 9 |

Repo: `https://github.com/OskarOG1/Kontent_bot.git` (`origin`, gałąź `main`). Wdrożenie na serwer nadal przez `wdroz.ps1` (część 7).

## Wymagania od właściciela (dopisane po planach)

1. Klipy i animacje (gify) mają być pobierane i cięte na krótkie wstawki, żeby dało się ich użyć w editach. Wpisane do `PLAN_EDITY_3_RENDER.md` (kontrakt `render.wstawki`, testy 9 do 11 w zadaniu 3.2, `gif` w typach klipów w zadaniu 3.3). Potwierdzone przez właściciela 2026-09-22: długość wstawki to mediana długości ujęć wzoru w granicach 0,5 do 2,0 s, kolejność rundami po materiałach w kolejności wysłania.

2. **Edity promują czapki marki 1993 Supply (2026-09-24).** Wzór `0914` to własny edit właściciela: przez cały edit ma znak wodny „1993 Supply” (51% szerokości, środek na 80% wysokości, krycie około 0,6), a na końcu stronę sklepu z czapką. Plik znaku leży w `dane/promocyjne/1993supply_watermark.png`. Wpisane jako zadanie 8.6 i decyzja 20 w mapie.

## Znane problemy i ograniczenia

00. **Tempo na realnym wzorze (2026-09-22).** Domyślne `beat_track` (start_bpm 120) dało 110 BPM, a ujęcia mają długości będące wielokrotnościami 0,36 s (około 165 BPM): tylko 23% cięć trafiało w uderzenie lub pół uderzenia (przypadkiem wychodzi około 29%). Z `start_bpm` od 140 w górę wychodzi 164,1 BPM i 77% cięć (71% na samych uderzeniach). To pomyłka 3:2, nie oktawa, więc pomiar A jej nie łapie. Decyzja właściciela (2026-09-22): `START_BPM = 150.0` w `src/analyze.py`. Po zmianie: 62 testy, pomiar A nadal 100% i błąd tempa maks 0,35%, `0915.mp4` daje 164,1 BPM i 77% cięć na uderzeniach lub półuderzeniach. Potwierdzone tylko na jednym realnym wzorze (właściciel ma ich dwa, w folderze `Edity` jest jeden).
0. **Detektor cięć rozstrzygnięty (odbiór 2026-09-22).** Zostaje ContentDetector z `min_klatek=3`. Na arkuszach `0915` AdaptiveDetector tnie ujęcie z szybkim ruchem kamery (23,6 do 25,1 s) na kawałki po 14 do 19 klatek, które nie leżą na uderzeniach (69% cięć w rytmie wobec 77%) i jest wolniejszy (56 s wobec 40 s). Na `0921` oba dają to samo. Ocena na dwóch wzorach.
0d. **Bardzo krótkie ujęcia we wzorach 60 fps.** `min_klatek=3` to przy 60 fps tylko 50 ms. Przejście typu glitch na końcu `0915` daje ujęcia po 4 i 8 klatek (0,07 i 0,13 s), które w renderze 30 fps staną się błyskiem 2 do 4 klatek. Jeśli w części 3 będzie to widać, minimalna długość ujęcia w sekundach (np. 0,1 s) zamiast w klatkach.
0e. `analizuj_rytm` woła `warnings.filterwarnings("ignore")`, co wycisza ostrzeżenia w całym procesie (także w testach). Drobne; do zawężenia przy okazji części 4, która i tak zmienia tę funkcję.
0a. **Biały błysk w środku ujęcia** daje fałszywe cięcie (test dodatkowy, poza planem, usunięty). Błysk między ujęciami działa. Zależy od realnych wzorów, czy to problem.
0b. **opencv na serwerze.** `scenedetect` 0.7.1 nie ma dodatku `opencv-headless` (plan go zakładał) i zależy twardo od `opencv-python`, który w obrazie Debian potrzebuje `libgl1` i `libglib2.0-0`. Do części 7 (Dockerfile).
0c. Pierwszy import librosa na świeżym środowisku trwa około 24 s (kompilacja numba), potem około 5 s. Pierwsze uruchomienie analizy na serwerze będzie wolne, limit 300 s to pokrywa.

1. **Limit 20 MB.** Zwykłe Bot API pozwala pobrać przez `getFile` najwyżej 20 MB, niezależnie od `LIMIT_POBIERANIA_MB` w `.env`. Właściciel ustawił tam 200, to nic nie zmienia, bo bot bierze mniejszą z dwóch wartości (`efektywny_limit_mb` w `src/bot.py`). Wzór wysłany na teście ręcznym przekraczał limit (trzy próby, puste katalogi w `dane/wzory/`), więc trzeba go zmniejszyć poniżej 20 MB albo poczekać na lokalny serwer Bot API (część 7, do 2000 MB). Przyczynę wnioskowaliśmy z dysku, nie z logu, więc nie jest potwierdzona.
2. Wcześniejsze puste projekty z testu ręcznego (23:03 i 23:05) mają prawdopodobnie tę samą przyczynę: klipy 19 do 31 s z telefonu łatwo przekraczają 20 MB. Też niepotwierdzone logiem.
3. Nieznany typ dokumentu w stanie zbierania (np. PDF) jest po cichu ignorowany, bez komunikatu.
4. Licznik trwających pobrań i aktywny projekt są jednym wspólnym stanem (bot obsługuje jednego właściciela i jedno zbieranie naraz). Przy wielu projektach naraz trzeba to przerobić.
5. **Pobieranie na serwerze: rozwiązane 2026-09-23.** Serwer Bot API działa jako UID 1000 (`bot-api/Dockerfile` z zadania 7.3, plus poprawka `1611573` na `delgroup`), a wzór 153 MB przeszedł całą drogę na produkcji. Tym samym punkt 1 tej listy dotyczy już tylko pracy lokalnej na zwykłym API.
6. **Narzut czasu nakładki `ekran` na granicy progu (2026-09-24, zadanie 8.4).** Pomiar `Pomiary/measure_nakladka.py`, sekcja B, na tej maszynie deweloperskiej: tryby `alfa` i `zielen` wygodnie poniżej 40% (widziane wartości od -45% do 61% w kolejnych przebiegach, zwykle ujemne albo kilkanaście procent), tryb `ekran` bywa nad progiem (54 do 95% w trzech przebiegach, jeden przebieg 59,1%, jeden w progu). Dwie przyczyny osobno potwierdzone: (a) czas absolutny pojedynczego przebiegu `przebieg_koncowy` na tej maszynie waha się nawet 2 do 3 razy między uruchomieniami tego samego kodu bez żadnej zmiany (test kontrolny: cztery identyczne przebiegi bez nakładki dały 79,4 do 82,1 s, ale osobne uruchomienia całego pomiaru dały bazę od 34 do 45 s) — więc procentowy próg jest z natury niestabilny na współdzielonej maszynie; (b) tryb `ekran` ma realnie droższy filtr niż `alfa`/`zielen`: `blend=all_mode=screen` operuje błędnie na surowym YUV (test na `render.py:409` potwierdził, że bez konwersji obraz wychodzi z fałszywymi kolorami), więc oba wejścia trzeba jawnie skonwertować przez `format=rgb24`, a wynik dociągnąć `scale=out_range=full` (bez tego biel na czerni wychodzi `(255,166,255)` zamiast `(255,255,255)`, `-color_range pc` na samym kodowaniu tego nie naprawia, sprawdzone eksperymentalnie). To dodatkowe przetwarzanie całej klatki jest prawdziwym, nie tylko szumowym kosztem. Zostawione jak jest: poprawność ważniejsza niż procent, a serwer produkcyjny (dedykowany, 2 CPU) powinien dać stabilniejszy pomiar niż ten laptop. Do decyzji właściciela: zaakceptować koszt trybu `ekran` czy szukać tańszej poprawki koloru później.
   Odbiór ponowny 2026-09-24: w drugim przebiegu pomiaru mediana z nakładką wyszła szybsza niż bez niej (alfa −45%, zielen −25%, ekran −15%), więc wynik mierzy szum maszyny. Od teraz progi narzutu liczy się z czasu procesora (blok WSPÓLNE, „Czas w pomiarach”).
   Odbiór 2026-09-24: `scale=out_range=full` koduje cały edit jako `yuvj420p` z `color_range=pc`. Zastępuje to `format=gbrp` na wejściach `blend` i `scale=out_range=tv,format=yuv420p` po nim (sprawdzone: biel 255, tło bez zmian, `yuv420p`). Pomiar B po zadaniu 8.5 liczy medianę z 3 przebiegów na przemian.
7. **`energia_uderzen` na syntetycznych klikach (odbiór 2026-09-24).** Uderzenia wypadają 2 do 3 ms po początku kliku, więc odcinek przed uderzeniem łapie początek następnego kliku. Na klikach 120 BPM ze skokiem w 9 s wygrywa uderzenie 8,503 s zamiast 9,002 s, a cięcie 9,0 wychodzi z zapasem 3 ms. Testy dropu idą więc na 150 BPM, gdzie uderzenia leżą 0,2 s od cięć. Na prawdziwej muzyce energia jest rozłożona i to nie ma znaczenia.
8. **Pliki w `dane/zdjęcia_bez_tła/` bez prawdziwej przezroczystości (2026-09-24).** Trzy pliki `Firefly_usun tło` są w RGB, bez kanału alfa: `32317` ma szachownicę wklejoną w obraz, `749723` białe tło, a `132215` czarne. Szachownica i biel wejdą do editu tak, jak są. Render kładzie prawdziwą przezroczystość na czarnym tle, więc czarne tło z `132215` wygląda tak samo. Pomiar bierze z tego katalogu tylko pliki z alfą. **Rozwiązane 2026-09-24:** właściciel usunął `32317` i `749723`. `132215` z czarnym tłem zostaje, bo wychodzi tak samo jak przezroczysty.
9. **Brak pamięci przy analizie utworu (2026-09-24, odbiór części 8).** Kod -9 na serwerze to `render.py` zabity przy 2,96 GB. `render.py` sam indeksuje nowe utwory, a `librosa.beat.beat_track` szacuje tempo na całej obwiedni z krokiem 128. Szczyt lokalnie: 2,8 do 3,0 GB dla utworów 2,5 min, około 4,0 GB dla 4 min 11 s. Limit 5 GB tylko odsuwa problem przy dłuższych utworach. Poprawka w zadaniu 4.5: tempogram liczony kawałkami, tempo przekazane do `beat_track`. Sprawdzona prototypem na 10 plikach: tempo i uderzenia identyczne, szczyt kroku rytmu 2871 MB wobec 507 MB. **Kod scalony 2026-09-24 na gałęzi `pamiec`:** `analizuj_dzwiek` na melodii 120 s ma szczyt 350,5 MB wobec 1509,8 MB przed poprawką, tempo bez zmian. Pomiar `Pomiary/measure_pamiec.py` (sekcje A i B) jeszcze do uruchomienia, limit `mem_limit: "5g"` zostaje do czasu wdrożenia.
10. **`/znak` przyjmuje PNG bez przezroczystości** i wtedy w edycie wychodzi półprzezroczysty prostokąt. Drobne; do ostrzeżenia w bocie przy okazji części 9.

## Decyzje techniczne podjęte przy realizacji

- Router aiogram budowany osobno dla każdego dispatchera (`zbuduj_router`), bo router można podpiąć tylko raz. Bez tego testy nie mogą tworzyć wielu dispatcherów.
- Filtr właściciela stoi na dispatcherze (`message` i `edited_message`), więc obcy nie uruchamia żadnego handlera, nie ma żadnych wywołań API.
- Licznik pobrań rośnie w pierwszej linii handlera materiału, przed jakimkolwiek `await`. To chroni `/gotowe` przed policzeniem albumu za wcześnie (pomiar B: 20 z 20).
- Błąd pobierania: `TelegramEntityTooLarge` daje komunikat o limicie, każdy inny wyjątek jest logowany (`log.exception`) i daje ogólny komunikat „nie udało się pobrać". Wcześniej każdy błąd był mylnie opisywany jako „za duży plik".
- Pobieranie przez `pobierz_plik` do `<cel>.part` z podmianą po sukcesie. To jedyne miejsce pobierania, część 7 je podmieni.
- Wyniki (`outputs/`), dane (`dane/`) i `.env` są poza gitem. Katalog `Pomiary/` od 2026-09-23 jest w repozytorium (decyzja właściciela), więc zmiany planów i dziennika trzeba commitować.

## Dziennik

### 2026-09-24 (zadanie 5.2: LUT przeniesienia barw, gałąź `kolor`)
- `src/kolor.py`: `polacz_statystyki` (średnia ważona liczbą próbek, odchylenie `sqrt(ważona średnia(odchylenie²+(średnia_j−średnia)²))` dokładnie z kontraktu), `indeksy_sekcji`/`cel_sekcji` (hak: ujęcia < `drop_ujecie`, montaż: ujęcia ≥ `drop_ujecie`, zawsze bez ostatniego ujęcia; bez `sekcje` jedna sekcja; ujęcia `probki: 0` pomijane, a gdy sekcja nie ma żadnego użytecznego ujęcia, bierze statystyki całości, czyli wszystkich ujęć oprócz ostatniego z tym samym filtrem `probki>0`), `lut_transferu` (siatka RGB → Lab, transfer Reinharda ze stosunkiem odchyleń przyciętym do [0,5; 2], zmieszanie z `sila`, powrót do sRGB z obcięciem), `zapisz_cube` (transpozycja osi `(b, g, r, kanał)` przed spłaszczeniem, żeby czerwony zmieniał się najszybciej, zgodnie ze specyfikacją `.cube`).
- **Siła 0 działa bez specjalnego przypadku:** wzór mieszania `lab*(1-sila) + przeniesiony*sila` daje tożsamość przy `sila=0` niezależnie od różnicy źródła i celu, więc nie trzeba osobnego warunku `if sila == 0`. Podobnie identyczne statystyki źródła i celu dają tożsamość przy dowolnej sile.
- Test 1 (tożsamość przez ffmpeg `lut3d` na gradiencie, rozmiar LUT 17, katalog roboczy `tmp_path`, ścieżka LUT względna) łapie od razu dwie rzeczy naraz: złą kolejność osi w `.cube` i błędną ścieżkę względną na Windows. Przeszedł od razu przy transpozycji `(2,1,0,3)`, bez debugowania złej kolejności.
- Testy: `tests/test_kolor.py` +10 (2 warianty tożsamości przez ffmpeg, LUT bez NaN/Inf i w zakresie z prawie jednolitego źródła, wzrost średniego b po LUT z ciepłym celem, 6 testów `cel_sekcji`: hak/montaż osobno, pominięcie ostatniego ujęcia, brak `sekcje`, pominięcie ujęć bez próbek, sekcja bez użytecznych bierze całość, zgodność odchylenia z ręcznym wyliczeniem). Razem 15 z 15 w tym pliku (0,7 s, bez dekodowania wideo).
- Cały zestaw: 207 z 207 w 155 s (197 + 10 nowych, zero regresji).
- Commit `kolor: LUT przeniesienia barw`.

### 2026-09-24 (zadanie 5.1: statystyki Lab ujęć wzoru, gałąź `kolor`)
- `src/kolor.py` (nowy): `rgb_do_lab`/`lab_do_rgb` (sRGB D65, standardowa macierz sRGB→XYZ IEC 61966-2-1, progi gamma 0,04045/0,0031308), `statystyki_obrazu` (średnia i odchylenie Lab po wszystkich pikselach, dowolny kształt wejścia z ostatnim wymiarem 3). Zgodność sprawdzona ręcznie: biel daje (100, 0, 0), czysta czerwień sRGB (53,28; 80,35; 67,22), obie w tolerancji 1 z kontraktu.
- `src/analyze.py`: `WERSJA_WZORU = 4`, `uruchom_probkowanie` (ffmpeg `fps=10,scale=135:240` do rawvideo, stdin/stdout zamknięte, stderr do pliku tymczasowego przez `NamedTemporaryFile(delete=False)`, plik zostaje na dysku jako log, nieusuwany), `statystyki_koloru` (przypisanie klatek do ujęć przez `indeks_ujecia`, ujęcie bez klatki bierze najbliższą czasowo pojedynczą klatkę i `probki: 0`). `analizuj_wzor` startuje próbkowanie przed `wykryj_ciecia` w `tempfile.TemporaryDirectory()`, czeka na nie (limit 300 s) dopiero po całej reszcie analizy (cięcia, dźwięk, drop), `finally` zawsze zabija i `wait()`-uje proces próbkowania, żeby katalog tymczasowy dało się usunąć na Windows i żeby wyjątek w dowolnym miejscu (np. `wykryj_ciecia`) nie zostawiał procesu ffmpeg.
- `tests/generuj.py`: `wideo_z_cieciami` dostał opcjonalny `kolory: list[tuple]`, nadpisujący domyślny `kolor_ujecia(numer)` (modulo długości listy) — potrzebne do testu z konkretnymi barwami (pomarańczowy/niebieski), bo automatyczne kolory z HSV nie dają kontroli nad kanałem b.
- Testy: `tests/test_kolor.py` (nowy, 5 testów: biel, czerwień, float 0..1, odwracalność `lab_do_rgb`, zerowe odchylenie na jednolitym kolorze) i `tests/test_analiza.py` (+8: dwa ujęcia pomarańcz/niebieski dają b>20/b<−20, ujęcie 0,05 s dostaje najbliższą klatkę i `probki: 0` — test bezpośrednio na `statystyki_koloru` z ręcznie złożonym plikiem klatek, bez ffmpeg, `--wszystkie` i CLI dają `wersja: 4` z `kolorystyka`, proces próbkowania nie zostaje żywy gdy `wykryj_ciecia` rzuci wyjątek; 3 istniejące testy `wersja: 3`→`4`, jedna nazwa testu przemianowana).
- Cały zestaw: 197 z 197 w 172 s (189 + 8 nowych, zero regresji). Weryfikacja zadania (`test_kolor.py` + `test_analiza.py`): 41 z 41 w 85 s.
- Commit `kolor: statystyki Lab ujęć wzoru`.

### 2026-09-24 (doprecyzowanie pomiaru części 5, przed startem zadań, gałąź `kolor`)
- Właściciel doprecyzował próg z zadania 5.1 „analiza z próbkowaniem najwyżej 1,3 raza dłuższa”: dotyczy czasu zegara (sprawdza równoległość próbkowania i wykrywania cięć), nie czasu procesora, bo drugie dekodowanie z definicji zwiększa czas procesora. Czas procesora analizy tylko do raportu.
- Czas zegara analizy: mediana z 5 przebiegów na przemian (z próbkowaniem i bez), próg oceniany tylko gdy rozrzut przebiegów bazowych poniżej 20%, inaczej wpis „niepewny” (zgodnie z zasadą z bloku WSPÓLNE).
- Narzut renderu przy sile 0,6 wobec 0 (zadanie 5.4, sekcja B) liczony z czasu procesora ffmpeg (`-benchmark`, `render.uruchom_ffmpeg` podmienione w pomiarze), jak w bloku WSPÓLNE.
- Stan przed startem: `main` zaktualizowany do `3108718` (PR #11, gałąź `pamiec` scalona), `python -m pytest -q` 189 z 189 (184 s). Gałąź `kolor` utworzona od `main`.
- Przy aktualizacji `main` w working tree pojawiły się niescommitowane zmiany `Pomiary/ROZWOJ.md` i `Pomiary/PLAN_EDITY_9_FABRYKA.md` (dopisek o zadaniu 9.5, skalowanie nakładki contain) sprzed sesji, niezwiązane z częścią 5. Odłożone na `git stash` (nie usunięte), żeby nie wmieszać ich w commity części 5.

### 2026-09-24 (zadanie 4.5: tempo liczone kawałkami, gałąź `pamiec`)
- `analyze.tempogram_sredni(obwiednia, sr)` liczy `librosa.feature.tempogram` kawałkami po `KAWALEK_TEMPOGRAMU = 4096` klatek obwiedni z zakładką `OKNO_TEMPOGRAMU // 2 = 192` z każdej strony, odciętą przed sumowaniem. `analizuj_dzwiek` liczy tempo z tego uśrednionego tempogramu (`librosa.feature.tempo`), a `beat_track` dostaje je jako `bpm=`, więc nie liczy własnego tempogramu na całej obwiedni.
- Test równoważności (`test_tempogram_sredni_rowny_pelnemu_liczonemu_naraz`): na sztucznej obwiedni 10 000 klatek wynik kawałkowy zgadza się z jednorazowym liczeniem na całości (`numpy.allclose`, `rtol=1e-6`).
- Test szczytu pamięci (`test_analizuj_dzwiek_szczyt_pamieci_ponizej_600mb`, `tracemalloc`, potwierdzone że widzi alokacje numpy w tym środowisku, więc nie trzeba podprocesu): melodia 120 s, próg 600 MB.
  - Przed poprawką (kod ze stashem, sprawdzone ręcznie skryptem poza pytest): szczyt 1509,8 MB, test nie przechodzi.
  - Po poprawce: szczyt 350,5 MB (23,5 s), tempo identyczne (120,2 BPM) w obu wersjach.
- Testy `python -m pytest -q`: 189 z 189 (173 s, dwa nowe testy doszły do 187 z części 8).
- **Pomiar `Pomiary/measure_pamiec.py` (`outputs/pomiar_pamiec.json`):** mierzy każdy plik i render w osobnym podprocesie (worker tego samego skryptu z flagą `--worker-*`), żeby szczyt pamięci jednego pliku nie zliczał się do następnego.
  - Sekcja A (5 utworów z `dane/muzyka/`, 5 wzorów z `dane/wzory/*.mp4`): tempo i liczba uderzeń identyczne stara i nowa drogą dla 10 z 10 plików. Szczyt analizy najdłuższego utworu (`L_amour toujours`, 4 min 11 s) 727,9 MB nową drogą wobec 3031,2 MB starą, próg 1024 MB w progu. Reszta utworów 253 do 542 MB nową drogą wobec 473 do 2033 MB starą.
  - Sekcja B (pełny montaż 1080x1920, wzór `0914`, nakładka syntetyczna `alfa` bo `dane/nakladki/` nie istnieje lokalnie, plansza `dane/promocyjne/Gemini_Generated_Image_o6f9c5o6f9c5o6f9.jpg`, znak `dane/promocyjne/1993supply_watermark.png`, biblioteka indeksowana od zera z kopii `dane/muzyka/`): szczyt 737,5 MB, próg 2048 MB w progu.
  - Oba progi zaliczone: `WYNIK: ZALICZONE`.

### 2026-09-24 (odbiór ponowny części 8 i przyczyna braku pamięci, Opus)
- **Werdykt: OK.**
  - Testy 187 z 187 (190 s).
  - Pomiar A: 5 z 5 wzorów, błąd maks 0,1 s (`0914` 8,5, `0915` 7,3, `0921` 5,4, `0922` 10,97, `0923` 11,23 s).
  - Arkusze: `0921` ma czysty hak i nakładkę od 5,5 s, plansza (czapka) zajmuje ostatnią sekundę bez nakładki i bez znaku.
  - Znak wodny w wyniku zgadza się z `0914` położeniem, wielkością i kryciem (porównanie klatek 2 i 12 s).
  - Kod bota: powrót do zbierania po `/nakladka`, `/plansza`, `/wzor` i `/znak`, pobieranie przed usunięciem starego pliku.
- **Uwagi, bez blokowania:**
  - pomiar B mierzy szum (narzut ujemny do −45%), zasada czasu procesora trafiła do bloku WSPÓLNE planów 4, 5, 6, 8 i 9;
  - nakładka na arkuszach to syntetyczne gwiazdy, a wygląd z flagą da dopiero zadanie 9.5;
  - w `0914` drop wyniku wypada w 9,87 s zamiast 8,5 s. Cięcia idą po uderzeniach wybranego utworu, a nakładka wchodzi razem ze skokiem energii utworu, więc obraz i dźwięk są zgodne ze sobą;
  - `/znak` z PNG bez alfy (znany problem 10).
- **Kod -9 na serwerze to nie nakładka.** Wpis z testu ręcznego przypisał wzrost pamięci części 8, ale zabity był proces `python` z 2,96 GB, a nakładka i znak to wejścia ffmpeg. Pomiar po kolei w jednym procesie dla 4-minutowego utworu:
  - wczytanie 201 MB, obwiednia 666 MB, `beat_track` 3031 MB, a reszta bez wzrostu;
  - szczyt rośnie z długością utworu (utwory 2,5 min 2,8 do 3,0 GB, 4 min 11 s około 4,0 GB), a render przelicza utwory, których nie ma w indeksie;
  - prototyp (tempogram kawałkami, tempo podane do `beat_track`) na 5 utworach i 5 wzorach: tempo i uderzenia identyczne, szczyt kroku rytmu 3,5 do 5,7 razy mniejszy, a analiza szybsza;
  - wpisane jako zadanie 4.5 w `PLAN_EDITY_4_MUZYKA.md`. Limit 5 GB może zostać do czasu jego wdrożenia.

### 2026-09-24 (test ręczny części 8: OOM przy montażu, limit pamięci podniesiony)
- Test ręczny właściciela na Telegramie po wdrożeniu 8.5/8.6: `/gotowe` dało „Montaż nie powiódł się: kod -9” dwa razy z rzędu (14:47 i 14:54).
- **Przyczyna (log serwera, `dmesg`):** `Memory cgroup out of memory: Killed process (python) anon-rss:2962848kB`. Proces `render.py` w kontenerze `bot` dobił do limitu `mem_limit: "3g"` z `docker-compose.yml` i został zabity SIGKILL. Serwer ma 7,6 GB RAM, w chwili zdarzenia wolne było 4,4 GB, więc to limit kontenera, nie brak pamięci na hoście.
- **Dlaczego dopiero teraz:** część 8 dokłada do jednego przebiegu ffmpeg dodatkowe strumienie (nakładka, znak wodny) obok materiałów właściciela, więc szczyt pamięci renderu wyraźnie wzrósł względem stanu z części 3/4, na którym limit 3 GB był dobrany.
- **Decyzja właściciela:** podnieść limit. `docker-compose.yml`: `mem_limit` z `3g` na `5g` dla usługi `bot`. Wdrożone przez `wdroz.ps1` (zwykła ścieżka, nie ręczna zmiana na serwerze, żeby nie zgubić się przy następnym wdrożeniu).
- Do obserwacji: czy 5 GB wystarczy przy większych materiałach (prawdziwe zdjęcia/klipy z telefonu bywają dużo cięższe niż próbki testowe); jeśli znowu kod -9, kolejny krok to zmierzenie realnego szczytu pamięci renderu (np. `docker stats` podczas testu), nie dalsze podnoszenie limitu na ślepo.

### 2026-09-24 (zadanie 8.5: cztery poprawki z odbioru)
- **Analiza:** `PROG_DROPU = 0.1`, szukanie od uderzenia 8 (było od 4, próg 0,2). Test 1 zadania 8.1 przepisany na 150 BPM ze skokiem do 0,85 (zamiast 120 BPM/0,15), wynik około 0,15 średniej energii, przy starym progu dałby `None`. Testy 2 do 5 zadania 8.1 bez zmian. Commit `analiza: próg i przedział dropu`.
- **`render.koniec_haka(plan, sekcje)`:** wydzielona z `okno_nakladki`, używana też jako start okna nakładki. Bez `sekcje` 40% `liczba_klatek` przyciągnięte do najbliższego początku ujęcia, z `sekcje.drop_ujecie` `klatka_od` pierwszego pasującego ujęcia, zawsze klamrowane do `[fps, liczba_klatek]`. Ten dolny klamer (co najmniej `fps`, czyli 1 s) dotyczy też `drop_ujecie: 0`: w testach z takim wzorem trzeba było przesunąć próbkowane klatki poza pierwszą sekundę, bo tam nakładka jeszcze nie wchodzi.
  - Tryb `ekran`: oba wejścia `blend` w `format=gbrp` (zamiast `rgb24`), po `blend` `scale=out_range=tv,format=yuv420p` (zamiast `out_range=full`, który dawał `yuvj420p`/`color_range=pc` na cały edit). `zweryfikuj_wynik` odrzuca teraz wynik, którego `pix_fmt` nie jest `yuv420p`.
  - Commit `render: koniec haka i zakres kolorów nakładki`.
- **Bot:** nowa `wroc_po_zapisie(state)` — gdy dane stanu mają `projekt_id`, wraca do `Stany.zbieram` z tym samym `projekt_id`, inaczej `state.clear()`. Zastąpiła `state.clear()` w `obsluz_nakladka_dokument`, `obsluz_plansza_zalacznik` i `obsluz_wzor_plik` (ten ostatni miał ten sam błąd od części 1). `usun_pliki_zasobu` dostała parametr `pomin`: nowy plik pobiera się najpierw pod docelową nazwą, dopiero potem usuwane są pozostałe pliki `<wzor_id>.*` tego rodzaju poza nim (odwrócona kolejność względem 8.3, żeby nieudane pobranie nie zostawiało wzoru bez zasobu). Commit `bot: stan projektu po nakładce i planszy`.
- **Pomiar (po zmianie układu `dane/` z wpisu poniżej):** `Pomiary/measure_nakladka.py` przepisany pod `dane/wzory/*.mp4` (zamiast `dane/probki/wzory/`), próg A na wszystkie znane wzory (dziś 5 z 5, `REFERENCJA_DROP_S` dostała `0914` i `0923`), sekcja B liczy medianę z 3 przebiegów na przemian (każda runda mierzy bez nakładki i wszystkie trzy tryby, potem `statistics.median` osobno dla każdej serii), materiał sekcji C to stała próbka z biblioteki właściciela (pierwsze 8 zdjęć, 4 nagrania, 2 zdjęcia bez tła z prawdziwą alfą przez `render.ma_alfa_z_pil`, twarde dowiązania do wspólnego katalogu projektu), plansza bez `dane/plansze/domyslna.*` bierze pierwszy plik bez przezroczystości z `dane/promocyjne/`. Uruchomiony na wszystkich 5 prawdziwych wzorach: **A 5 z 5 w granicy 0,5 s** (błędy 0,0 do 0,1 s), **B w progu** (mediany narzutu ujemne na tym przebiegu, bo maszyna akurat liczyła równolegle pełny zestaw testów — potwierdza tylko niestabilność z „Znanych problemów” punkt 6, nie regresję), **C pięć arkuszy** `outputs/porownanie_nakladka_*.png`. Testy 187 z 187. Commit `Pomiary: pomiar nakładki po poprawkach`.

### 2026-09-24 (zadanie 8.6: znak wodny marki)
- **`render.py`:** `wymiary_i_pozycja_znaku(sciezka, szerokosc, wysokosc)` liczy skalę i pozycję z prostokąta nieprzezroczystych pikseli (PIL `getchannel("A").getbbox()`): szerokość bboxa skalowana na `UDZIAL_SZEROKOSCI_ZNAKU = 0.51` szerokości wyniku, środek bboxa po skalowaniu wyśrodkowany poziomo i ustawiony na `POZYCJA_ZNAKU_PION = 0.8` wysokości. `przebieg_koncowy` dostał `znak`/`znak_do_s`: znak to kolejne wejście (`-loop 1 -t <okno>`), filtr `scale=<dw>:<dh>,format=rgba,colorchannelmixer=aa=0.65` (`KRYCIE_ZNAKU`), potem `overlay=<x>:<y>:enable='between(t,0,<okno>)'` na wierzchu tego, co już jest (materiał, ewentualnie nakładka). Bez znaku polecenie bez zmian (input index liczony dynamicznie przez `wejscia.count("-i")`, nie na sztywno). `renderuj` liczy okno znaku przez nową `okno_znaku(plan, plansza_uzyta)` (od klatki 0 do początku planszy albo do końca), dokłada `"znak": bool` do podsumowania. CLI `--znak`.
- **Bot:** `/znak` (stan `Stany.czekam_na_znak`) przyjmuje tylko dokument z rozszerzeniem `.png`, zapisuje pod stałą nazwą `dane/znak_wodny.png` (jeden plik dla wszystkich wzorów, nie per-wzór jak nakładka/plansza), `/znak usun` usuwa. Po zapisaniu wraca przez `wroc_po_zapisie` jak nakładka i plansza po 8.5. `renderuj_w_tle` i `obsluz_cmd_gotowe` dokładają `--znak`, gdy `dane/znak_wodny.png` istnieje. `/status` dostał wiersz `komunikaty.status_znaku`.
- **Pomiar:** `Pomiary/measure_nakladka.py`, sekcja C: nowa `znajdz_znak_wodny()` (najpierw `dane/znak_wodny.png`, inaczej `dane/promocyjne/1993supply_watermark.png`), przekazywana do `render.renderuj` dla każdego wzoru. Właściciel jeszcze nie wysłał własnego znaku botem, więc pomiar użył pliku promocyjnego.
- Uruchomiony na wszystkich 5 prawdziwych wzorach po zmianie: **A 5 z 5** (bez zmian, znak nie dotyka analizy), **B w progu** (mediany narzutu ujemne również w tym przebiegu — ta sama niestabilność maszyny co w 8.5, teraz bez równoległych testów w tle, więc to cecha maszyny, nie artefakt obciążenia), **C pięć arkuszy**, tym razem ze znakiem wodnym widocznym na każdym. Testy 187 z 187. Commit `render i bot: znak wodny marki`.
- **Część 8 (zadania 8.5 i 8.6) zamknięta pod względem kodu.** Zostaje właścicielowi: `dane/znak_wodny.png` (własny plik zamiast promocyjnego zastępnika), `wdroz.ps1`, `docker compose exec bot python src/analyze.py --wszystkie`, test ręczny (`/nakladka`, `/plansza`, `/znak`, `/nowy`, materiały, `/gotowe`), ocena arkuszy `outputs/porownanie_nakladka_*.png`.

### 2026-09-24 (flaga jako nakładka, napisy z `0923` do planu 9, Opus)
- **Flaga UE:** sprawdzone na klatce z materiałem, obok klatki `0914` z 12 s (`scratchpad`, porównanie trybów).
  - Klip `European Union Flag ｜EU Flag Motion Background｜` z `dane/nagrania/` z kryciem 50% daje granat i półprzezroczyste gwiazdy jak we wzorach `0914` i `0923`.
  - Tryb `ekran` rozjaśnia cały kadr, a właśnie ten tryb dałby fladze `tryb_nakladki` z części 8.
  - Wcześniejsza uwaga, że flaga się nie nada, była błędna. Nie nada się tylko w obecnych trybach.
  - Tryb `krycie` najpierw trafił do planu 8 jako zadanie 8.7. Wycofany, bo plan 8 jest w trakcie wykonania (prośba właściciela). Jest teraz w planie 9 jako zadanie 9.5, do zrobienia zaraz po scaleniu części 8 na gałęzi `krycie`.
- **Napisy z `0923`:** klatki 0,5 do 13,6 s, dopisane do planu 9 jako zadania 9.6 do 9.8:
  - pojedyncze słowa pismem odręcznym, złote z granatowym obrysem, na środku kadru, zmieniane mniej więcej co uderzenie;
  - ostatnie słowo wlatuje z dużego powiększenia tuż po dropie (11,23 s);
  - pionowy napis szeryfowy przy lewej krawędzi, pisany litera po literze w haku.
  - W pomiarze treść to hasło właściciela, nie tekst ze wzoru.
  - Gwiazd wokół twarzy (śledzenie postaci) nie ma w planach.
- **Pliki:** właściciel usunął dwa pliki bez prawdziwej przezroczystości (znany problem 8).

### 2026-09-24 (nowy układ `dane/`, wzory `0914` i `0923`, cel promocyjny, Opus)
- **Układ `dane/` od właściciela:**
  - `muzyka` (5 utworów);
  - `nagrania` (29 klipów, około 11 GB, głównie 4K);
  - `wzory` (5 plików mp4 luzem);
  - `zdjęcia` (40);
  - `zdjęcia_bez_tła` (9, z czego 6 z alfą);
  - `promocyjne` (czapka, zdjęcia z czapką, znak wodny).
  - `dane/probki/` zniknął. Blok WSPÓLNE we wszystkich planach 4, 5, 6, 8 i 9 opisuje nowy układ i stałą próbkę materiałów do arkuszy; wzory leżą w `dane/wzory/*.mp4`.
  - Luźne pliki w `dane/wzory/` nie przeszkadzają botowi, bo `najnowszy_wzor`, `/status` i `--wszystkie` biorą tylko podkatalogi z `wzor.json`.
  - `wdroz.ps1` pakuje `git archive main`, więc `dane/` nie jedzie na serwer.
- **Nowe wzory:**
  - `0914`: 30,8 s, 175,2 BPM, drop 8,50 s, ujęcie 6. Na arkuszu gwiazdy UE wchodzą w 8,5 s. Hak to postać w czapce przez 3,7 s, potem montaż z napisem przed dropem;
  - `0923`: 26,1 s, 130,8 BPM, drop 11,23 s, ujęcie 6. Nakładka gwiazd wchodzi około 11,25 s. Wzór ma też napisy słowo po słowie i gwiazdy wokół postaci przed dropem, czego plany nie odtwarzają.
  - Reguła z zadania 8.5 trafia na wszystkich 5 wzorach, więc próg A w planie 8 to teraz 5 z 5.
- **Cel promocyjny:** właściciel wyjaśnił, że edity promują czapki. Stąd zadanie 8.6 (znak wodny według pomiaru z `0914`, bez znaku na planszy, na wierzchu warstw). W planie 6 napisy leżą pod znakiem i kończą się najwyżej na 75% wysokości.

### 2026-09-24 (odbiór części 8, PR #9, Opus)
- Werdykt: poprawki. Kod jest zgodny z planem i testy przechodzą (172 z 172, 164 s), ale na prawdziwych wzorach wychodzi błąd, którego pomiar nie złapał, bo sekcje A i C się nie uruchomiły.
- **Drop na prawdziwych wzorach** (liczone na zapisanych analizach `outputs/wzor_*.json`, bo `wykryj_drop` bierze tylko pola z wersji 2):
  - `0915`: 7,30 s, ujęcie 8 (referencja 7,2);
  - `0922`: 10,97 s, ujęcie 2 (referencja 11,0);
  - `0921`: brak dropu, bo najlepszy wynik to 0,129 średniej energii przy progu 0,2. Bez `sekcje` nakładka szła od pierwszej klatki i przykrywała hak.
  - Formalnie 2 z 3 mieści się w progu A, ale błąd w `0921` jest poważny.
- **Nowa reguła (sprawdzona na tych samych danych i na syntetycznych klikach):** szukanie od uderzenia 8, próg 0,1.
  - Prawdziwe wzory: 7,30, 5,40 i 10,97 s, czyli 3 z 3;
  - stała głośność (90, 120 i 150 BPM): najwyżej 0,065;
  - stała melodia: 0,023;
  - skok w drugiej połowie: 0,009.
  - Przy szukaniu od 4 stała głośność dawała 0,114, bo pierwszy odcinek zaczyna się po pierwszym kliku.
- **Tryb `ekran`:** wynik `yuvj420p` z `color_range=pc` na cały edit. Zamiennik sprawdzony w scratchpadzie.
- **Bot:**
  - `/nakladka` i `/plansza` w trakcie zbierania kasują `projekt_id` (`state.clear()`), więc `/gotowe` odpowiada `BRAK_STANU`. Ten sam błąd ma `/wzor` od części 1;
  - stary plik jest usuwany przed pobraniem nowego.
- **Test 1 zadania 8.1** przechodzi z zapasem 3 ms (znany problem 7), więc zmienia się na 150 BPM.
- **Plany:**
  - `PLAN_EDITY_8_NAKLADKA.md`: zadanie 8.5 z czterema poprawkami, testami i commitami; kontrakty z dopiskami „poprawka po odbiorze”; próg A to teraz 3 z 3;
  - `PLAN_EDITY_6_TEKST.md`: `koniec_haka` przeniesiona do `render` (powstaje w 8.5), test 7 przepisany, bo nakładka nie leci już od pierwszej klatki.
- **Od właściciela przed 8.5:** skopiować `dane/0915.mp4`, `dane/0921.mp4` i `dane/0922.mp4` do `dane/probki/wzory/`, bo bez tego sekcje A i C znów się pominą.

### 2026-09-24 (część 8, zadanie 8.4: pomiar, zamknięcie części 8 pod względem kodu)
- `Pomiary/measure_nakladka.py` według układu `measure_muzyka.py`: sekcja A (`dane/probki/wzory/`, drop wobec referencji z arkuszy klatek `0915`/`0921`/`0922`, cache w `outputs/wzor_<nazwa>.json`, próg 2 z 3 w granicy 0,5 s), sekcja B (syntetyczna, zawsze dostępna: `przebieg_koncowy` na 20 s szumu 1080x1920, z nakładką każdego trybu i bez, próg narzutu 40%), sekcja C (`dane/probki/wzory/` i `dane/muzyka/`: pełny render z nakładką i planszą, arkusz `outputs/porownanie_nakladka_<wzor>.png`, nakładka i plansza przez `magazyn.plik_zasobu` z zapasem `domyslna.*`, a bez tego syntetyczne gwiazdy (PIL, wielokąty gwiazd zapisane jako mov z kodekiem png) i syntetyczna plansza „KONIEC” (PIL, jednolite tło i tekst)).
- Sekcje A i C pominięte lokalnie: `dane/probki/wzory/` nie istnieje w tym środowisku (tak jak we wszystkich poprzednich częściach). Ścieżka sprawdzona ręcznie na tymczasowym syntetycznym wzorze (usunięty po teście, `dane/` poza gitem): `wykryj_drop` znalazł `drop_s 9.0, drop_ujecie 9` zgodnie z zadaniem 8.1, `renderuj` z syntetyczną nakładką gwiazd i planszą „KONIEC” przeszedł bez błędu, arkusz porównawczy powstał. Kod ścieżek A i C uznaję za sprawdzony, mimo braku prawdziwych wzorów do oceny referencji.
- **Odkrycie: pierwsza wersja materiału do sekcji B (jednolity kolor, jak w innych pomiarach części 8) dawała fałszywie wysoki narzut (41 do 98%).** Jednolity kolor koduje się tak szybko (2 do 4 s dla 20 s materiału), że stały koszt dekodowania i kompozycji drugiego wejścia (nakładki) dominuje procentowo, niezależnie od samej nakładki. Zamienione na szum (jak sekcja B pomiaru części 3), która koduje się 30 do 45 s: narzut spada do realistycznego zakresu. Mimo to wynik jest niestabilny między uruchomieniami na tej maszynie, patrz „Znane problemy” punkt 6.
- Testy: bez zmian względem zadania 8.3 (172 z 172), zadanie 8.4 nie dotyka `src/` ani `tests/`.
- **Część 8 zamknięta pod względem kodu.** Zostaje właścicielowi: pliki nakładek i plansz (`dane/nakladki/`, `dane/plansze/`), ocena dropu na arkuszach porównawczych po realnym pomiarze na serwerze, `wdroz.ps1`, potem `docker compose exec bot python src/analyze.py --wszystkie`, test ręczny (`/nakladka`, `/plansza`, `/nowy`, materiały, `/gotowe`).

### 2026-09-24 (część 8, zadanie 8.3: bot)
- `src/magazyn.py`: `ROZSZERZENIA_NAKLADEK` i `plik_zasobu(katalog_danych, rodzaj, wzor_id)` (plik wzoru ma pierwszeństwo przed `domyslna`, przy kilku rozszerzeniach wygrywa najnowszy czas modyfikacji).
- `src/bot.py`: nowe stany `czekam_na_nakladke`/`czekam_na_plansze`; `/nakladka` i `/plansza` (z podkomendą `usun`) sprawdzają najpierw `magazyn.najnowszy_wzor`, bez wzoru dają `BRAK_WZORU`. Nakładka akceptuje tylko `message.document` (inny typ załącznika dostaje prośbę o wysłanie jako plik, bo Telegram kompresuje zdjęcia i wideo wysłane inaczej, tracąc przezroczystość), zapisywana jako `dane/nakladki/<wzor_id>.<ext>` po usunięciu starych plików tego wzoru, odpowiedź podaje tryb z `render.tryb_nakladki` na już zapisanym pliku. Plansza używa `rozpoznaj_zalacznik` (przyjmuje więc też zdjęcie), zapisywana do `dane/plansze/`. `renderuj_w_tle` i `obsluz_cmd_gotowe` dokładają `--nakladka`/`--plansza` do wywołania `render.py`, gdy `magazyn.plik_zasobu` coś znajdzie. `podsumuj_wzor` przekazuje `dane.get("sekcje")` do `komunikaty.podsumowanie_wzoru`. `/status` dokłada wiersz o nakładce i planszy aktywnego wzoru (`magazyn.najnowszy_wzor`), pomijany gdy nie ma żadnego wzoru.
- `src/komunikaty.py`: `POMOC`/`KOMENDY` opisują `/nakladka` i `/plansza`; nowe teksty (`nakladka_prosba`, `nakladka_zapisana`, `NAKLADKA_WYSLIJ_JAKO_PLIK`, `NAKLADKA_NIEPOPRAWNY_TYP`, `NAKLADKA_USUNIETA`, `plansza_prosba`, `PLANSZA_NIEPOPRAWNY_TYP`, `PLANSZA_USUNIETA`, `PLANSZA_ZAPISANA`, `status_zasobow_wzoru`); `podsumowanie_wzoru` dostał parametr `sekcje` i dokłada klauzulę „drop w X s (ujęcie N)” albo „bez dropu” do tego samego zdania (nie osobną linię, inaczej niż `linia_muzyki` w części 4), bo plan podaje przykład z małej litery jako fragment zdania.
- **Decyzja własna: brak limitu rozmiaru pobierania dla nakładki i planszy.** Kontrakt „Bot” w planie opisuje limit `efektywny_limit_mb` tylko przy wzorze i materiałach, nie przy nakładce/planszy. Zostawione bez limitu, żeby nie dodawać niezleconego zachowania; do rozważenia przy realnym użyciu, jeśli właściciel wyśle bardzo duży plik.
- Testy: `tests/test_magazyn.py` +4 (`plik_zasobu`: pierwszeństwo wzoru, nowszy z dwóch rozszerzeń, brak plików, `domyslna` jako zapasowy), `tests/test_bot.py` +13 zgodnie z listą zadania (w tym prawdziwy plik PNG z `generuj.nakladka_testowa` jako treść pobrania, żeby `render.tryb_nakladki` miał co rozpoznać). Cały zestaw 172 z 172 w około 189 s.

### 2026-09-24 (część 8, zadanie 8.2: nakładka i plansza w renderze)
- `src/render.py`: `tryb_nakladki` (PNG/GIF przez PIL, reszta przez ffprobe `pix_fmt` z listą formatów z alfą plus znacznik `alpha_mode` VP9, inaczej próbka pierwszej klatki na zieleń, próg 30%), `segment_planszy` (split na tło rozmyte `boxblur` cover i pierwszy plan contain, `overlay` wyśrodkowany, zdjęcie przez `przygotuj_zdjecie`, klip z `tpad` na zamrożenie ostatniej klatki), nakładka w `przebieg_koncowy` jako trzecie wejście (`-loop 1` dla PNG, inaczej `-stream_loop -1`, oba z `-t` do długości okna, `setpts` na przesunięcie startu okna), `overlay` z `enable='between(t,...)'` dla trybów alfa i zielen (zielen dodatkowo `colorkey`), `blend=all_mode=screen` dla ekranu. Bez nakładki polecenie identyczne jak przed częścią 8 (test 7). `renderuj` dostał `nakladka`/`plansza`, licząc okno nakładki (`okno_nakladki`) i pozycję dropu w wyniku (`pozycja_dropu_w_planie`) niezależnie od tego czy plik nakładki podano. Plansza zastępuje segment ostatniego ujęcia tylko gdy ujęć jest więcej niż jedno. CLI dostało `--nakladka`/`--plansza`.
- **Odkrycie: `blend=all_mode=screen` po konwersji `format=rgb24` i ponownym zakodowaniu do `yuv420p` dawał złe kolory (biały róg wychodził `(255,166,255)`, niebieskie tło `(132,0,255)` zamiast bez zmian).** Sam wynik `blend` w RGB tuż po filtrze był poprawny (sprawdzone przez zrzut surowego RGB bez kodowania) — błąd wchodził dopiero przy kodowaniu do `yuv420p` bez jawnego zakresu kolorów (ffmpeg zgadywał zakres niespójnie między kodowaniem a dekodowaniem). Naprawa: `scale=out_range=full` zaraz po `blend`, wymusza pełny zakres i naprawia oba przypadki. Overlay (tryby alfa i zielen) nie miał tego problemu, więc tam nic nie zmieniałem.
- `magazyn.typ_pliku` wymaga dwóch argumentów (`nazwa`, `mime`), nie jednego — poprawione w `segment_planszy` na `typ_pliku(sciezka.name, None)`.
- `tests/generuj.py`: `nakladka_testowa(sciezka, czas_s, tryb, rozmiar, fps)` z czterema trybami z zadania (`alfa` mov+png+rgba, `zielen` i `ekran` mp4 rgb24, `png` statyczny obraz PIL).
- Testy: `tests/test_nakladka.py`, 8 testów zgodnie z listą zadania (270x480, materiały jednolicie niebieskie zamiast domyślnego czerwono-niebieskiego podziału, żeby łatwo odróżnić nakładkę od materiału). Cały zestaw 157 z 157 w około 166 s.

### 2026-09-23 (część 8, zadanie 8.1: drop w analizie)
- `src/analyze.py`: `WERSJA_WZORU = 3`, `wykryj_drop(uderzenia_s, energia_uderzen, ciecia_s, ciecia_uderzenia, czas_s)` dokładnie według kontraktu (szukanie od uderzenia 4 do czasu `czas_s/2`, wynik `srednia(E[i:i+8]) - srednia(E[i-8:i])` z oknami przyciętymi ale minimum 4 wartości, wygrywa najwyższy wynik z remisem na najwcześniejsze uderzenie, brak przy mniej niż 16 uderzeniach albo wyniku poniżej `0,2 * średnia energii`, przyciągnięcie do najbliższego cięcia o indeksie od 1 w granicy 1 s, inaczej pierwsze cięcie po czasie uderzenia). `analizuj_wzor` dokłada pole `sekcje` (`None` bez dropu).
- Test 5 z listy zadania napisany zgodnie z poprawioną wersją planu (150 BPM, skok w 8,9 s) — zweryfikowany, że moja implementacja `wykryj_drop` daje dokładnie `drop_s 9.0` i `drop_ujecie 9`, zgodnie z poprawką. Przy okazji potwierdzone na własnym eksperymencie (przed poprawką w planie), że wariant 120 BPM/9,4 s rzeczywiście daje wynik na granicy milisekund (uderzenie 9,501 s, remis odległości do cięć 9 i 10), więc poprawka w planie była trafna.
- Testy: `tests/test_analiza.py` 31 z 31 (6 nowych zgodnie z listą zadania, plus 3 istniejące testy zaktualizowane z `wersja: 2` na `wersja: 3`, jeden przemianowany `..._daje_wersje_2` na `..._daje_wersje_3`). Cały zestaw 149 z 149 w około 110 do 134 s.
- Stan wejściowy zgodny z planem: `main` na `dc46f0b` (PR #7 scalony), `python -m pytest -q` 144 z 144 przed startem. Gałąź `nakladka` utworzona od `main`.

### 2026-09-23 (część 8 w toku: poprawka testu 5 w zadaniu 8.1 i decyzja 14 w mapie, Opus)
- Właściciel: część 4 scalona (PR #7), część 8 w toku. Jego wyjaśnienie wyłączenia rozpoznania dźwięku wzoru („za mało plików”) nie zgadza się z pomiarem z zadań 4.2 i 4.4. NIEWŁAŚCIWY utwór przekraczał próg: 0,829 dla `0915` i 0,76 dla `0922`. Przesunięcie wychodziło 116 s zamiast 40,36 s. Więcej podobnych utworów tego nie naprawi. Decyzja 14 w mapie dostała dopisek o wyłączeniu, tabela części ma stan 4 i 8.
- **Błąd w planie 8, test 5 zadania 8.1.** Wariant 120 BPM ze skokiem głośności w 9,4 s daje pierwsze głośne uderzenie w 9,501 s, prawie dokładnie w połowie między cięciami 9 i 10. Sprawdzone na `analizuj_dzwiek` i roboczym `wykryj_drop` Sonneta: wynik 10,0 zamiast oczekiwanych 9,0, więc test nie przejdzie niezależnie od implementacji. Nowy wariant w planie to 150 BPM ze skokiem w 8,9 s: uderzenie 9,202 s, wynik 9,0 i ujęcie 9, sprawdzone tak samo. Test 1 (120 BPM, skok w 9 s) daje 9,0 i ujęcie 9, bez zmian.

### 2026-09-23 (część 4, zadanie 4.4: pomiar muzyki i arkusz porównawczy, zamknięcie części 4)
- `Pomiary/arkusz.py`: `arkusz_porownawczy(wzor, wynik, cel, klatek_na_s=2.0, kolumny=16, komorka=(108,192))` — N klatek równo rozłożonych w czasie każdego pliku (jedno wywołanie ffmpeg na plik, filtr `fps=N/czas_pliku`), wiersz wzoru nad wierszem wyniku, cienka jasna linia między kolejnymi parami. Używa opencv (już zależność projektu przez `scenedetect`).
- `Pomiary/measure_muzyka.py`: sekcja A (syntetyczna, 10 melodii 60 s, 3 fragmenty każda z losowym przesunięciem, rozpoznanie liczone bezpośrednio przez `music.dopasuj_odcisk` skoro `wybierz_utwor` nie wywołuje go od decyzji w zadaniu 4.2; plus błąd tempa klików 60 do 180 BPM), sekcja B (prawdziwe pliki: czas indeksowania, tabela utworów, macierz zgodności jako dane diagnostyczne, wybór `wybierz_utwor` dla każdego wzoru), sekcja C (determinizm), sekcja D (render bez `--utwor` i arkusz porównawczy). Progi B i C dostosowane do wyłączonego trybu `dzwiek_wzoru`: zamiast starych referencji (40,36 s / 23,29 s) sprawdzane jest, że każdy wzór dostaje wybór i że tryb to `tempo`.
- Wynik: sekcja A 0/30 błędnie rozpoznanych, błąd przesunięcia maks 6,07 ms (mediana 3,9 ms, próg 15 ms), błąd tempa maks 0,72% (próg 2%), zgodność najniższej pasującej pary 0,923, najwyższej niepasującej 0,522 — na syntetycznych, wyraźnie różnych melodiach `dopasuj_odcisk` działa dobrze z zapasem.
- **Sekcja B potwierdza liczbowo decyzję z zadania 4.2.** Macierz zgodności na prawdziwych plikach: `0915` kontra „Hot N Cold” 0,949, kontra „Ex's And Oh's” 0,829 — oba nad `PROG_ZGODNOSCI` (0,5), więc rozpoznanie po odcisku wybrałoby błędny utwór albo trafiało przypadkiem. `0922` kontra „Ex's And Oh's” 0,965 (właściwy), kontra „Hot N Cold” 0,76 — też nad progiem. To ta sama para liczb co w eksploracji przy zadaniu 4.2, teraz odtworzona przez właściwy pomiar. Wszystkie trzy wzory (`0915`, `0921`, `0922`) dostały tryb `tempo`: `0915` i `0921` trafiły na „Hot N Cold” (164,1 BPM), `0922` na „Ex's And Oh's” (175,2 BPM), wszystkie z mnożnikiem 1,0 (tempa wzorów i utworów są already blisko siebie). Sekcja C: dwa przebiegi identyczne. Sekcja D: trzy arkusze `outputs/porownanie_muzyka_*.png` powstały (materiały z generatora, bo `dane/probki/materialy/` puste — zgodnie z regułą zastępczą z bloku WSPÓLNE).
- Analiza trzech prawdziwych wzorów (4K): 77,4 s (`0915`), 64,5 s (`0921`), 132,6 s (`0922`, największy plik). Indeksowanie biblioteki (2 utwory z rytmem + `MR.mp4` bez dźwięku, pominięty z ostrzeżeniem): 20,9 s.
- Cały zestaw testów 144 z 144 (150 s). Commit `Pomiary: pomiar muzyki i arkusz porównawczy`.
- **Część 4 zamknięta pod względem kodu.** Zostaje właścicielowi: `wdroz.ps1`, potem na serwerze `docker compose exec bot python src/analyze.py --wszystkie` i `docker compose exec bot python src/music.py indeksuj`, oraz test ręczny na Telegramie (`/nowy`, materiały, `/gotowe` przy `0915` albo `0922`; oczekiwany podpis to teraz „dobór po tempie X BPM, od Y s”, NIE „dźwięk wzoru od”, bo ten tryb jest wyłączony).

### 2026-09-23 (część 4, zadanie 4.3: render i bot)
- `src/render.py`: `plan_ujec` dostał `przesuniecie_s` (gdy podane, cięcia to `ciecia_s` wzoru i koniec to `zrodlo.czas_s`, `start_audio_s = przesuniecie_s`) oraz pole `numer_wzoru` na każdym ujęciu (indeks cięcia wzoru, przeskakuje przy ujęciach o 0 klatkach). `renderuj` ma nowy parametr `muzyka: Path | None`, `utwor` jest teraz opcjonalny; nowa `przygotuj_zrodlo_dzwieku` ustala tryb (`staly` dla `--utwor`, albo `dzwiek_wzoru`/`tempo`/`bez_rytmu` z `music.indeksuj` + `music.wybierz_utwor` dla `--muzyka`) i odpowiednie parametry dla `plan_ujec`; oba puste dają `RuntimeError("Brak utworu i katalogu muzyki")`. Podsumowanie `utwor` dostało `tryb`, `zgodnosc`, `tempo_bpm`, `mnoznik` obok istniejących `plik`/`start_s`. CLI: `--utwor` opcjonalny, nowy `--muzyka`.
- Ponieważ tryb `dzwiek_wzoru` jest wyłączony (decyzja z zadania 4.2), w praktyce `przygotuj_zrodlo_dzwieku` dziś zwraca tylko `tempo` albo `bez_rytmu` dla `--muzyka`; gałąź `dzwiek_wzoru` zostaje w kodzie (przekazuje `przesuniecie_s` do `plan_ujec`), gotowa na wypadek ponownego włączenia rozpoznawania w `music.wybierz_utwor`.
- `src/komunikaty.py`: `BRAK_UTWORU` zastąpione przez `BRAK_MUZYKI` („Biblioteka muzyki jest pusta. Dodaj utwory do dane/muzyka.”). `podsumowanie_renderu` dostał drugą linię (`linia_muzyki`) zależną od `tryb`: „dźwięk wzoru od X s” (dziś nieużywane), „dobór po tempie X BPM, od Y s” (`tempo`), „od początku” (`bez_rytmu`, `staly` i inne). Nowa `status_muzyki(n)` → „Muzyka: N utworów.”.
- `src/bot.py`: `renderuj_w_tle` przyjmuje `katalog_muzyki` i przekazuje `--muzyka` zamiast `--utwor`. `obsluz_cmd_gotowe` sprawdza `music.pliki_muzyki(katalog_muzyki)` zamiast istnienia `staly.mp3`; pusta biblioteka daje `komunikaty.BRAK_MUZYKI` i nie dodaje zadania. `obsluz_cmd_status` dokłada linię `komunikaty.status_muzyki(...)` (bez wywoływania analizy/indeksowania, tylko `pliki_muzyki`).
- Testy: `tests/test_render.py` +5 (przesunięcie_s i numer_wzoru w dwóch wariantach, pełny render bez `--utwor` kończy się trybem `tempo`, brak `--utwor` i brak `--muzyka` daje `RuntimeError`/jedną linię na stderr), istniejące testy `--utwor` bez zmian zachowania. `tests/test_bot.py`: zastąpiony test `staly.mp3` testem pustej biblioteki z nowym komunikatem, dodane testy `--muzyka` zamiast `--utwor` w wywołaniu render.py i liczby utworów w `/status`; `podsumowanie_renderu_testowe` i asercja podpisu zaktualizowane pod nowy dwuliniowy format (`tryb: "tempo"`, bo `dzwiek_wzoru` niedostępny). Cały zestaw 144 z 144. Commity `render: utwór z biblioteki i dźwięk wzoru` i `bot: muzyka z biblioteki`.

### 2026-09-23 (część 4, zadanie 4.2: biblioteka i rozpoznanie dźwięku wzoru)
- `src/music.py`: `ROZSZERZENIA_MUZYKI`, `pliki_muzyki`, `indeksuj` (indeks wersja 2, dogrywa tylko nowe/zmienione pliki po rozmiarze i czasie modyfikacji, usuwa skasowane, pomija pliki bez dźwięku z logiem), `dopasuj_odcisk`, `PROG_ZGODNOSCI = 0.5`, `wybierz_mnoznik` (odległość tempa z oktawą), `zagesc_mnoznikiem` (mnożnik 2 i 0,5 na siatce uderzeń utworu), `waz_kandydata_tempo` (dopuszczalne przesunięcia `s` bez ekstrapolacji na końcu), `znajdz_start_uderzenie` (korelacja profilu energii), `wybierz_utwor` (kolejność trybów, `ValueError` z liczbą odrzuconych) i CLI `indeksuj`/`wybierz`.
- **Odkrycie: `librosa.beat.beat_track` z domyślnym `trim=True` ucina uderzenia w cichszych fragmentach sygnału (patrz wpis zadania 4.1) — to samo uderzyło tutaj w test profilu energii (test 5 z listy zadania), naprawione już w 4.1 (`trim=False`), bez dodatkowych zmian w 4.2.**
- **Duży problem: dopasowanie po odcisku dźwięku (`dzwiek_wzoru`) nie działa wiarygodnie na prawdziwej bibliotece właściciela.** Kontrakt każe liczyć jedną globalną korelację Pearsona na spłaszczonym oknie chromy (znormalizowanym per pasmo). Ta metoda dosłownie z kontraktu dawała na prawdziwej parze `0915` kontra `Hot N Cold (Hardstyle)` tylko 0,50 do 0,60 w okolicy właściwego przesunięcia (40,36 s), zamiast referencyjnych 0,78 z przeglądu planów (wpis „przegląd planów 4 do 6”), i test 1 z zadania 4.2 (syntetyczna melodia, fragment własnego nagrania) też nie przechodził (0,557 zamiast wymaganych 0,8). Przyczyna: cisza między dźwiękami rozcieńcza pojedynczą globalną korelację (około połowy ramek to cisza o praktycznie losowej znormalizowanej chromie). Zamieniłem zgrubną metodę na średnią podobieństwa kosinusowego liczoną ramka po ramce (z pominięciem ramek o zerowej normie po obu stronach), zachowując dokładne dostrojenie po obwiedni bez zmian. To naprawiło syntetyczny test (0,93 zamiast 0,557 dla trafienia, około 0,45 dla nietrafienia, poniżej `PROG_ZGODNOSCI`), ale na prawdziwej bibliotece właściciela (`Hot N Cold (Hardstyle)` i `Ex's And Oh's (Hardstyle)`, oba hardstyle) dalej zawodzi: metoda po ramkach namierzała zupełnie inne przesunięcie (116 s zamiast 40,36 s) z wysoką zgodnością dla OBU utworów (0,95 i 0,83), bo głośne, mocno zniekształcone przejścia (dropy) są w obu utworach szerokopasmowe/szumopodobne i mylą dopasowanie chromy niezależnie od treści melodycznej. Separacja harmoniczno-perkusyjna (`librosa.effects.hpss`) przed liczeniem chromy naprawiła przesunięcie (40,40 s, zgodne z referencją), ale zgodność z NIEWŁAŚCIWYM utworem została wysoka (0,78, ponad `PROG_ZGODNOSCI`) — te dwa realne utwory są zbyt podobne harmonicznie (oba hardstyle, podobne prowadzenie melodii), a HPSS jest też kosztowny (около 50 s na 3 krótkie pliki testowe, zagrażałoby budżetowi „10 s na utwór” z indeksowania).
- **Decyzja właściciela (2026-09-23, po przedstawieniu wyników): tryb `dzwiek_wzoru` wyłączony na razie, `wybierz_utwor` zawsze wybiera po tempie.** `wybierz_utwor` nie wywołuje już `dopasuj_po_dzwieku`, niezależnie od tego czy wzór ma `odcisk_dzwieku`: od razu tryb `tempo` (a bez tempa `bez_rytmu`). `dopasuj_odcisk`, `zgodnosc_ramek`, `dopasuj_po_dzwieku` i `PROG_ZGODNOSCI` zostają w kodzie (przetestowane, poprawne na wyraźnie różnych nagraniach syntetycznych), gotowe do ponownego podłączenia, gdyby w przyszłości pojawiła się lepsza metoda albo bardziej zróżnicowana biblioteka. Zaktualizowany opis w `PLAN_EDITY_4_MUZYKA.md` (sekcja „Efekt”). Test 1 zadania 4.2 przepisany na bezpośrednie wywołanie `dopasuj_odcisk` (wciąż działa na syntetykach) plus nowy test potwierdzający, że `wybierz_utwor` nigdy nie zwraca `tryb: "dzwiek_wzoru"`.
- **Skutek dla referencji z przeglądu planów:** liczby „`0915` od 40,36 s”, „`0922` od 23,29 s” (opisane jako wynik trybu `dzwiek_wzoru`) już nie obowiązują jako oczekiwany wynik `wybierz_utwor` — realne wzory pójdą w tryb `tempo`. Do przeliczenia w zadaniu 4.4 (sekcja B): tam progi trzeba będzie dostosować do trybu `tempo`, nie do starych referencji dźwiękowych.
- Przy okazji naprawiony bug w generatorze testowym: `tests/generuj.py:melodia` używała stałego ziarna (`np.random.default_rng(0)`) dla szumu w kliku, niezależnie od parametru `ziarno` — każde wywołanie `melodia(...)` dawało identyczny klik, co sztucznie zawyżało podobieństwo między różnymi (rzekomo niepowiązanymi) melodiami w testach. Szum klika teraz pochodzi z tego samego generatora co wysokość tonów (zależnego od `ziarno`).
- Testy: `tests/test_muzyka.py` z 15 testami (rozpoznanie fragmentu przez `dopasuj_odcisk`, wyłączenie trybu `dzwiek_wzoru` w `wybierz_utwor`, przejście do trybu `tempo` dla nagrania spoza biblioteki, `dopasuj_odcisk` przy wzorze dłuższym niż utwór, mnożnik 2 i 0,5, profil energii zamiast najgłośniejszego okna, remis tempa, brak kandydata z liczbą odrzuconych, cztery warianty `indeksuj`, determinizm `wybierz_utwor`), 15 z 15. Cały zestaw 136 z 136 w około 120 s (jeden dodatkowy test w `test_kolejka.py` bywa niestabilny czasowo pod obciążeniem maszyny, niezwiązany ze zmianami tej sesji, potwierdzone przejściem w izolacji). Commit `muzyka: biblioteka i rozpoznanie dźwięku wzoru`.

### 2026-09-23 (część 4, zadanie 4.1: odcisk dźwięku i energia uderzeń)
- Stan wejściowy: `main` na `09e441c` (PR #6 scalony, plany 4 do 9 w repo), `python -m pytest -q` 116 z 116 w 67,6 s. `git diff --stat 57ba8fe -- src/ tests/` pusty, więc kotwice `plik:linia` z planu są aktualne. Gałąź `muzyka` utworzona od `main`.
- `src/analyze.py`: `WERSJA_WZORU = 2`, nowe stałe `KROK_CHROMA = 2048` i `KROK_OBWIEDNI = 256`. `analizuj_dzwiek(sciezka)` dekoduje raz i liczy tempo/uderzenia (jak dawny `analizuj_rytm`), `energia_uderzen` (RMS sygnału między kolejnymi uderzeniami) i `odcisk` (chroma i obwiednia do dopasowania w części 4.2); wszystkie wywołania librosy w jednym `warnings.catch_warnings()` (zamiast globalnego `warnings.filterwarnings("ignore")` ze znanego problemu 0e). `analizuj_rytm` zostaje nakładką na `analizuj_dzwiek`. `analizuj_wzor` woła `analizuj_dzwiek` raz i dokłada `energia_uderzen`/`odcisk_dzwieku` do wyniku. CLI dostał `--wszystkie [--katalog-danych dane]` (`main_wszystkie`): przelicza katalogi `wzory/<id>/` z `zrodlo.*`, pomija bez błędu katalogi bez źródła, drukuje liczby przeliczonych/pominiętych/błędów, kod 0 gdy brak błędów.
- **Odkrycie: `librosa.beat.beat_track` domyślnie ma `trim=True`, co ucina uderzenia w cichszych fragmentach.** Test energii (`glosnosc=[(0, 0.1), (10, 1.0)]`, mnożnik 10x między częściami) z domyślnym `trim` dawał `uderzenia_s` wyłącznie od 10 s w górę: cała cicha część (0 do 10 s) znikała z wyniku, mimo że kliki tam realnie są (sprawdzone ręcznie w scratchpadzie na samym `klik()`, bez melodii, ten sam efekt). To psułoby tryb `tempo` w części 4.2 (profil energii wymaga uderzeń na całej długości wzoru i utworu, nie tylko głośnej części). Naprawa: `trim=False` w wywołaniu `beat_track` w `analizuj_dzwiek`. Po zmianie pełne pokrycie (40 uderzeń na 20 s zamiast 20). Pozostałe testy analizy (stałogłośne kliki) bez zmian wyniku.
- **Decyzja własna: `melodia()` skaluje mnożnikiem `glosnosc` cały sygnał uderzenia (klik i ton), nie tylko ton.** Plan nie precyzuje, czy `glosnosc` ma osłabiać też klik pomocniczy. Przy stałym klik-u i zmiennym tylko tonie test energii i tak przechodzi (klik dominuje RMS przy krótkim oknie), ale `trim=False` czyni to nieistotne dla wykrywania rytmu: klik działa identycznie w obu wariantach. Zostawione jak jest (mnożnik na całości), prostsze i zgodne z treścią kontraktu „ton (...) do tego klik (...), żeby tracker znalazł tempo” + `trim=False` gwarantuje wykrycie niezależnie od głośności.
- **Decyzja własna: istniejący test `test_wideo_bez_dzwieku_ma_puste_pola_rytmu` zaktualizowany (`wersja: 1` → `2`, dodane asercje na nowe pola `null`).** Kontrakt zadania wymaga jednocześnie „`wzor.json` w wersji 2” i „dotychczasowe testy analizy przechodzą bez zmian”: te dwa wymagania są sprzeczne dla jednej literalnej asercji liczby wersji. Zinterpretowane jako „zachowanie testów bez zmian”, nie dosłowny zakaz edycji linii z numerem wersji.
- Nowe testy w `tests/test_analiza.py` (6, zgodnie z listą w zadaniu): kształt i typy odcisku, energia uderzeń odzwierciedla profil głośności, brak dźwięku daje `None`/`null`, `analizuj_wzor` na melodii daje `wersja: 2`, `--wszystkie` przelicza tylko katalogi ze źródłem, `analizuj_rytm` nie zmienia globalnych filtrów `warnings`. `tests/generuj.py`: `profil_glosnosci` (pomocnicza), `klik(..., glosnosc=None)`, `melodia(...)`, `wideo_z_cieciami(..., dzwiek=None)`.
- Testy: `tests/test_analiza.py` 26 z 26, cały zestaw 122 z 122 w 71,3 s (116 + 6 nowych, zero regresji). Commit `analiza: odcisk dźwięku i energia uderzeń`.

### 2026-09-23 (przepisanie planów 4 do 9, Opus)
- Właściciel wybrał wszystkie opcje z przeglądu: A (dźwięk wzoru), B (nakładka i plansza), C (kolor na sekcję), D (napisy w haku), E (kolejność i fabryka), F (odbiór na arkuszu porównawczym). Edity robi z materiałów i wzorów, które mu dostarczają.
- Przepisane: `PLAN_EDITY_0_MAPA.md` (cel, tabela części, kolejność 4, 8, 5, 6, 9, decyzje 3, 4, 6, 7, 8, 10 i 11 zmienione, nowe 14 do 19, ryzyka, zakres), `PLAN_EDITY_4_MUZYKA.md`, `PLAN_EDITY_5_KOLOR.md`, `PLAN_EDITY_6_TEKST.md`.
- Nowe: `PLAN_EDITY_8_NAKLADKA.md` (drop, nakładka, plansza) i `PLAN_EDITY_9_FABRYKA.md` (restart, biblioteka wzorów, warianty, partie, `/ponow`).
- Najważniejsze zmiany kontraktów:
  - `plan_ujec` dostaje `przesuniecie_s` i pole `numer_wzoru` w ujęciach;
  - `wzor.json` rośnie wersjami: 2 (odcisk dźwięku, energia uderzeń), 3 (`sekcje`), 4 (`kolorystyka` na ujęcie);
  - `analyze.py --wszystkie` przeniesione do części 4;
  - `START_BPM = 150` dla wszystkiego;
  - LUT na segment zamiast w przebiegu końcowym;
  - kolejność warstw: materiał z kolorem, nakładka, napisy;
  - zadanie 5.0 i `licencje.csv` zdjęte.
- Blok WSPÓLNE ujednolicony we wszystkich nowych planach:
  - kotwice z `57ba8fe`;
  - Ubuntu 24.04;
  - przed push sprawdzane tylko `.env`, `dane/` i `outputs/`;
  - czas testów nie jest progiem;
  - bez odwołania do `KONTEKST_PROJEKTU.md`;
  - arkusz porównawczy w każdym pomiarze.
- Plany 1, 2, 3 i 7 bez zmian (wykonane). Zmiany planów na gałęzi `plany-4-9` w PR do `main`, na prośbę właściciela.

### 2026-09-23 (przegląd planów 4 do 6 pod cel „fabryka editów”, Opus)
- Dźwięk wzorów porównany z biblioteką korelacją chromy i obwiedni uderzeń (skrypt jednorazowy poza repo): `0915` to „Hot N Cold (Hardstyle)” od 40,36 s (r chromy 0,78, drugi utwór 0,12), `0922` (nowy wzór, 4K, 120 fps) to „Ex's And Oh's (Hardstyle)” od 23,29 s (r 0,81 wobec 0,12), `0921` nie pasuje do żadnego (r do 0,07). Biblioteka właściciela to więc dźwięki samych wzorów.
- Reguła planu 4 „najmocniejszy fragment” (średnia RMS uderzeń w oknie długości wzoru) sprawdzona na tych utworach: w „Hot N Cold” najmocniejsze okno zaczyna się w 39,31 s, czyli 3 uderzenia przed miejscem ze wzoru; w „Ex's And Oh's” w 115,08 s, a okno wzoru jest dopiero 158. z 347. Dzisiejszy render bierze najwcześniejsze możliwe uderzenie (`znajdz_start_uderzenia`), więc edit do `0915` idzie na intro utworu, a wzór leży od 40 s.
- Tempo przy `start_bpm` 120 i 150: „Ex's And Oh's” 88,3 i 175,2 BPM (pomyłka oktawy przy 120), „Hot N Cold” 164,1 w obu, `0915` 110,0 i 164,1, `0921` 139,7 w obu, `0922` 88,3 i 175,2. Przy 150 wszystkie pięć plików wychodzi spójnie.
- Arkusze klatek trzech wzorów (4 klatki na sekundę): ten sam format we wszystkich. Hak z napisem w pierwszych 4 do 11 s w naturalnych kolorach, potem drop, od którego wchodzi pełnoekranowa nakładka graficzna (gwiazdy) i mocny grading (`0915` i `0922` niebieski z posteryzacją, `0921` złoty), na końcu plansza około 1 s. Napisy tylko w haku, szeryfowe, białe, na środku kadru. Jedna średnia barw z całego wzoru (plan 5) wymiesza ciepły hak z niebieskim montażem.
- Rozbieżności w planach: zadanie 5.1 i „Gotowe, gdy” części 5 opierają się na odwołanym zadaniu 5.0; blok WSPÓLNE w planach 4 do 6 każe sprawdzać, że `Pomiary/` nie jest w indeksie (od 2026-09-23 jest) i trzymać testy poniżej 60 s (próg zniesiony), podaje Ubuntu 22.04 (serwer ma 24.04) i odsyła do nieistniejącego `KONTEKST_PROJEKTU.md`.
- Stan lokalny: `main` 1 commit za `origin/main` (scalenie PR #5). W `dane/` brak `muzyka/staly.mp3` i `wzory/`, pliki mają dopisek „ (1)”, doszły `probki/wzory/0922.mp4`, `gify/` i `zdjęcia/`.
- Do decyzji właściciela: część 4 przebudowana na dopasowanie do dźwięku wzoru (utwór i przesunięcie z korelacji, dobór po tempie tylko jako zapas), nowa część na nakładkę i planszę końcową, kolor na ujęcie albo sekcję w części 5, okna napisów w haku w części 6, poprawki bloku WSPÓLNE.

### 2026-09-23 (zadanie 3.7: dźwięk przycinany na wejściu, długość wyniku wymuszona)
- `src/render.py`, `przebieg_koncowy`: usunięty `atrim`+`asetpts`+`-shortest`. Utwór jest teraz przycinany opcjami wejścia `-ss <start_audio_s> -t <czas_trwania_s>` przed drugim `-i`, filtr dźwięku ma już tylko `afade` na wyciszenie. Długość wyjścia wymuszona jawnie: `-frames:v <liczba_klatek>` na strumieniu wideo i `-t <czas_trwania_s>` jako opcja wyjścia (obcina też dźwięk do dokładnej długości, niezależnie od zaokrągleń kodera AAC do ramek 1024 próbek). Reszta funkcji bez zmian (maxrate, bufsize, x264-params).
- Test `test_renderuj_utwor_wielokrotnie_dluzszy_od_editu_ma_pelna_dlugosc` w `tests/test_render.py`: pełny render z utworem 60 s (klik 128 BPM) przy edicie z 4 zdjęć i wzoru dającego około 1,9 s (dokładnie z `plan_ujec`). Sprawdza liczbę klatek wideo co do jednej (`ffprobe -count_frames`) i osobno długość strumienia wideo i strumienia dźwięku (`ffprobe stream=duration` z `-show_streams`, nie `format=duration`), oba w tolerancji 1 klatki. Bez tego testu przypadek był cichy, bo dotychczasowe testy renderu używały utworu zbliżonego długością do editu.
- Potwierdzone na tych samych plikach z diagnozy: obraz 960 klatek/32,0 s, dźwięk teraz też dokładnie 32,0 s (oba `stream=duration` się zgadzają), zamiast wcześniejszego niedeterministycznego 817 do 830 klatek.
- **Czas testów: 63,9 do 98,5 s w dwóch przebiegach na tej samej maszynie, ten sam kod.** Nowy test sam w sobie kosztuje 6,5 do 10,5 s (zależnie od obciążenia), ale całość testów skacze bardziej niż to tłumaczy (druga obserwacja tego samego zjawiska co w zadaniu 3.6: `test_rytm_zwraca_typy_pythona`, niezmieniany, wahał się między 8,2 a wcześniej 10,1 s). Próg 60 s z planu jest teraz przekraczany w gorszych warunkach. Właściciel potwierdził 2026-09-23: czas testów nie ma znaczenia, próg nie jest już twardym wymogiem.
- Pomiar: sekcja A dostała `czas_strumienia_wideo_s` i `czas_strumienia_audio_s` w `format` (oba z `ffprobe stream=duration`), bez zmian w progach. Wynik: 39/39 granic, mediana 8,0 ms, format OK, oba strumienie 19,366 do 19,367 s (zgodne). Sekcja B: 36,48 MB (próg 47,5 MB). Sekcja C pominięta lokalnie (brak `dane/probki/materialy/` i projektów).
- Commit `render: dźwięk przycinany na wejściu i wymuszona długość wyniku`.

### 2026-09-23 (wdrożenie naprawy 3.7 i powtórzenie testu na projekcie 20260923_163553)
- `wdroz.ps1 -Serwer root@46.62.151.181 -Wymus` (`-Wymus`, bo commit `e16c604` nie był jeszcze na GitHubie): oba obrazy przebudowane, `docker compose up -d --build`, bot wstał i loguje `Start polling` jako `@cwel54_bot`.
- Bez ponownego wysyłania materiałów: `docker compose exec bot python src/render.py --wzor dane/wzory/20260923_143107/wzor.json --projekt dane/projekty/20260923_163553 --utwor dane/muzyka/staly.mp3 --wyjscie dane/projekty/20260923_163553/wynik.mp4 --limit-mb 200` na istniejącym projekcie z diagnozy. Kod wyjścia 0, `wynik.json`: 32,0 s, 32 ujęcia, 5 materiałów, 27,15 MB, czas renderu 141 s.
- `ffprobe` na `wynik.mp4`: strumień wideo `duration=32.000000` (960 klatek), strumień audio `duration=32.000000` (1498 ramek AAC). Oba strumienie zgodne co do sekundy, zgodnie z naprawą 3.7 i z pomiarem A z tego samego zadania. Wcześniej (przed naprawą) ten sam projekt dawał 817 do 830 klatek dźwięku i błąd `zweryfikuj_wynik`.
- `projekt.json` na serwerze skorygowany ręcznie po potwierdzeniu właściciela (obejrzał `wynik.mp4` przesłany do wglądu): `stan: "gotowy"`, `wynik: "wynik.mp4"`, `blad: null`. Wcześniejsza blokada zapisu przez zdalną powłokę wynikała z trybu auto (zapis na serwerze produkcyjnym bez jawnej zgody), nie z problemu technicznego.
- Test ręczny części 3 zamknięty: właściciel potwierdził wynik jako OK.

### 2026-09-23 (test ręczny części 3 na serwerze: montaż odrzucony, diagnoza)
- Właściciel wysłał `/nowy`, 3 zdjęcia i 2 klipy (10,9 s i 19,6 s), `/gotowe`. Bot odpowiedział „Montaż nie powiódł się: Długość wyniku nie zgadza się z planem”, projekt `20260923_163553` ma stan `blad`.
- Diagnoza na serwerze z katalogu `praca/`, który zostaje po błędzie: plan i sklejony materiał były poprawne (960 klatek, dokładnie 32,0 s, tyle co wzór), a `wynik.mp4` miał 826 klatek i 27,5 s. Czyli błąd siedzi w `przebieg_koncowy`, a `zweryfikuj_wynik` zadziałał tak, jak miał: złapał krótszy plik zamiast wysłać go po cichu.
- Przyczyna ustalona eksperymentami na tych samych plikach: przycinanie utworu filtrem `atrim` razem z `-shortest` przy utworze wielokrotnie dłuższym od editu (152 s wobec 32 s) kończy plik za wcześnie i niedeterministycznie (817, 826 i 830 klatek w kolejnych przebiegach), z ostrzeżeniami „Queue input is backward in time” i „Non-monotonic DTS”. Okładka w mp3 nie jest winna (sprawdzone na kopii bez okładki), `-t` zamiast `-shortest` naprawia tylko obraz. Działa przycięcie utworu opcjami wejścia `-ss` i `-t` przed `-i`: obraz 960 klatek i 32,0 s, dźwięk 32,0 s.
- Naprawa opisana jako zadanie 3.7 w `PLAN_EDITY_3_RENDER.md`, razem z testem, który to łapie (dziś testy używają utworu długości editu, więc przypadek nie występował).
- Po naprawie nie trzeba wysyłać materiałów jeszcze raz: render można powtórzyć na serwerze z CLI na istniejącym projekcie `20260923_163553`.

### 2026-09-23 (odbiór części 3 i wdrożenie, Opus)
- Werdykt: OK. Testy 115 z 115 lokalnie w 56,0 s (próg 60 s, margines wąski, potwierdzam obserwację wykonawcy) oraz 115 z 115 w kontenerze na serwerze w 62 s, czyli render działa też na ffmpegu 7.1.5 z Debiana, nie tylko na lokalnym 8.1.
- Pomiar A w progach: 39 z 39 granic (100%), mediana odległości cięć od rytmu 8 ms przy progu 20 ms, format zgodny (H.264 High, yuv420p, SAR 1:1, 30 fps, AAC 48 kHz stereo). Pomiar B: 36,4 MB przy progu 47,5 MB. Sekcja C pominięta lokalnie (brak materiałów), więc arkusze `outputs/render_*.png` nie powstały: zastępuje je test ręczny właściciela na serwerze.
- Sprawdzone w kodzie: `plan_ujec` liczy granice z czasów bezwzględnych; `przebieg_koncowy` jest jedynym miejscem kodowania wyniku, a `maxrate` liczy się z limitu i długości; każde wywołanie ffmpeg i ffprobe ma zamknięty stdin; wejście zdjęcia ograniczone `-frames:v`; z katalogu wzoru render czyta wyłącznie `wzor.json`; `wstawki` układa kawałki rundami; w bocie `--limit-mb` pochodzi z `efektywny_limit_wysylki_mb`.
- Rozstrzygnięcie pytania wykonawcy o `ValueError` przy za krótkim utworze: zostaje jak jest. Przez ekstrapolację w `czas_z_pozycji` realny za krótki utwór nie wywoła tego wyjątku, tylko skończy się błędem z `zweryfikuj_wynik` („Długość wyniku nie zgadza się z planem”). Właściwe sprawdzenie należy do części 4, gdzie `wybierz_utwor` wymaga, żeby utwór mieścił całe okno wzoru na swojej siatce uderzeń. Do rozważenia przy okazji części 4: czytelniejszy komunikat, gdy okno nie mieści się w utworze.
- Uwaga na części 4 do 6: czas testów to dziś 56 s przy progu 60 s z planów. Każda kolejna część dokłada testy do tego samego zestawu, więc albo skracamy najdroższe (utwór testowy, `test_cli_sukces`), albo świadomie podnosimy próg.
- Wdrożenie po scaleniu PR #4: `wdroz.ps1` na `46.62.151.181`, obraz przebudowany z `pillow` 12.3.0 i `pillow-heif` 1.8.0, oba kontenery wstały, bot poluje jako `@cwel54_bot`. Wzór z 14:31 i `staly.mp3` leżą na serwerze, więc test ręczny można robić od razu.

### 2026-09-23 (część 3, zadanie 3.6: pomiar, zamknięcie części 3)
- `Pomiary/measure_render.py`: sekcja A (syntetyczna, 1080x1920, wzór 40 cięć co jedno uderzenie, 10 zdjęć w tym 2 z obrotem EXIF, 1 PNG z przezroczystością, 1 HEIC, 3 klipy w tym 1 z obrotem i 1 krótszy od ujęcia, utwór klik 124 BPM 60 s jako mp3), sekcja B (sam `przebieg_koncowy`, bez segmentów, na 60 s szumu 1080x1920, limit 50 MB), sekcja C (realne wzory z `dane/probki/wzory/`, analiza z cache w `outputs/wzor_<nazwa>.json`, materiały z `dane/probki/materialy/` albo z najnowszego projektu z co najmniej 3 materiałami, arkusz `outputs/render_<wzor>.png`). Wynik w `outputs/pomiar_render.json`.
- **Odkrycie: `ContentDetector` nie widzi cięć między dwoma na przemian powtarzającymi się kolorami.** Pierwsza wersja materiałów sekcji A (klipy jako jednolite kolory z `klip_testowy`) dała tylko 25 z 39 granic (64%), mimo że render był poprawny: piksele na granicy zmieniały się dokładnie tam, gdzie powinny (sprawdzone ręcznie klatka po klatce). Zawężone do minimalnego powtarzalnego przypadku: wideo naprzemiennie A, B, A, B, ... (12 ujęć, dwa jednolite, wyraźnie różne kolory) daje `wykryj_ciecia` tylko jedno cięcie (czas 0), zero granic wewnętrznych, mimo że AdaptiveDetector czy proste porównanie pikseli widzą różnicę bez trudu. Przyczyna leży w samym `ContentDetector` (PySceneDetect), nie w `render.py`: powtarzanie się tej samej sceny po kilku ujęciach myli detektor, prawdopodobnie jakiś mechanizm tłumienia „migotania". To nie dotyczy prawdziwego produktu (`wykryj_ciecia` w części 2 analizuje wyłącznie oryginalny wzór właściciela, nigdy własny render), tylko metody pomiaru: przy 3 klipach w kółko (rundy `wstawki`) i solidnych kolorach łatwo trafić w ten wzorzec. Naprawa w materiałach pomiaru: każdy kawałek `wstawki` musi mieć inny kolor niż poprzedni, więc klip do rund użyty w sekcji A ma teraz `wideo_z_cieciami` z cięciami co dokładnie `dlugosc_wstawki_s` (0,5 s), żeby żadna para kolejnych kawałków tej samej rundy nie powtarzała koloru; pozostałe dwa klipy (z obrotem, krótszy od ujęcia) skrócone poniżej `dlugosc_wstawki_s`, więc dają dokładnie jedną wstawkę i nigdy się nie powtarzają. Po zmianie: 39 z 39 (100%).
- Wyniki: sekcja A 100% granic (39/39), mediana odległości od rytmu 8 ms (próg 20 ms), pełna zgodność formatu (H.264 High, yuv420p, SAR 1:1, 30 fps, AAC 48 kHz stereo), render 1,8 do 2,9 s na sekundę wyniku w zależności od obciążenia maszyny. Sekcja B: 36,4 MB (próg 47,5 MB), czas 60 do 118 s zależnie od obciążenia maszyny podczas przebiegu (patrz niżej). Sekcja C lokalnie pominięta (brak `dane/probki/materialy/` i brak projektów w `dane/projekty/`), ale ścieżka kodu sprawdzona ręcznie na prawdziwym wzorze `0921.mp4` z tymczasowymi materiałami zastępczymi: analiza 72 s, render 52 s, 16 ujęć, podsumowanie i arkusz PNG poprawne (usunięte po sprawdzeniu, w repo i w `outputs/` zostaje tylko `wzor_0921.json` jako cache). Pełny przebieg sekcji C na materiałach właściciela zostaje do zrobienia przez właściciela albo w kolejnej sesji z dostępem do `dane/probki/materialy/`.
- **Czas testów: 55 do 74 s w zależności od obciążenia maszyny, dwa razy tuż nad progiem 60 s z planu.** Zaraz po zadaniu 3.5 cały zestaw szedł 55,2 s (zmierzone wtedy). Po serii pomiarów sekcji C na prawdziwym 4K wzorze (dużo dysku i CPU naraz) `pytest -q` zaczął iść 67 do 74 s przy tym samym kodzie testów, mimo obciążenia CPU z powrotem na poziomie 15% w spoczynku: `test_rytm_zwraca_typy_pythona` (istniejący test części 2, niezmieniany, zimny import librosa) sam podskoczył z 5,3 do 9,8 do 10,1 s między pomiarami. Wygląda na spowolnienie dysku (prawdopodobnie skaner antywirusowy reagujący na serię nowych i zmienionych dużych plików `.mp4` w `outputs/`), nie na regresję w kodzie: te same operacje, ten sam wynik testów (115 z 115 za każdym razem). Dodatkowa poprawka wykonana mimo to: test CLI rozdzielony na dwa (`test_cli_sukces` przez prawdziwy podproces, `test_cli_blad_jedna_linia_na_stderr` przez bezpośrednie wywołanie `render.glowna` w tym samym procesie z `capsys`), bo druga połowa poprzedniego testu płaciła za zimny start Pythona bez potrzeby sprawdzania czegokolwiek poza kodem wyjścia i jedną linią na stderr. Zysk z tej zmiany samej w sobie mały (około 1 s), bo dominuje `test_cli_sukces` (podproces z prawdziwym renderem) i niezależny od mojego kodu `test_rytm_zwraca_typy_pythona`. **Do sprawdzenia przy odbiorze:** uruchomić `python -m pytest -q` na spokojnej maszynie (bez równoległego I/O) i potwierdzić, czy czas rzeczywiście mieści się poniżej 60 s w typowych warunkach; jeśli nie, kandydaci do dalszego cięcia to skrócenie utworu testowego w `tests/test_render.py` poniżej 6 s albo zgoda właściciela na podniesienie progu w planie.
- Commit `Pomiary: pomiar renderu` (plan nie podał nazwy commitu dla zadania 3.6, lista „Commity” ma tylko 5 pozycji dla zadań 3.1 do 3.5; nazwa dobrana według stylu innych commitów `Pomiary:`).

### 2026-09-23 (część 3, zadanie 3.5: montaż w bocie)
- `src/bot.py`: `obsluz_cmd_gotowe` po sprawdzeniu materiałów dokłada dwa nowe warunki, oba bez dodawania zadania do kolejki: brak `magazyn.najnowszy_wzor` daje `komunikaty.BRAK_WZORU`, brak `dane/muzyka/staly.mp3` daje `komunikaty.BRAK_UTWORU`. Dalej zapisuje `wzor_id` w `projekt.json` (pole już istniało w schemacie od części 1), ustawia stan `w_kolejce`, odpowiada `projekt_w_kolejce(..., pozycja)` (nowy parametr, ten sam wzorzec zapowiedzi co przy analizie: `zapowiedz.set()` w `finally`) i dodaje `renderuj_w_tle` do `kolejka_obiekt`. `renderuj_w_tle`: stan `renderowanie`, komunikat „Montuję.”, `kolejka_modul.uruchom` na CLI `render.py` z `--limit-mb` z `efektywny_limit_wysylki_mb` (istniejąca funkcja z 7.2, nie tworzona drugi raz), `limit_s=900`. Błąd albo przekroczony czas: stan `blad`, pole `blad` z pierwszą linią stderr (albo stałym tekstem przy przekroczeniu czasu), odpowiedź tą samą treścią. Sukces: `answer_document` z podpisem z `komunikaty.podsumowanie_renderu`, `TelegramEntityTooLarge` przy wysyłce daje `limit_rozmiaru` (rozmiar realnego pliku, ten sam komunikat co przy pobieraniu), inny wyjątek loguje i daje `komunikaty.BLAD_WYSYLKI`; dopiero po udanej wysyłce stan `gotowy` i `wynik` w `projekt.json`.
- Trzy drobne braki z „Znanych problemów” (punkt 3 i dwa wpisy z odbioru części 1 i 2), wszystkie w zakresie zadania: `obsluz_material` odpowiada `komunikaty.MATERIAL_NIEPOPRAWNY_TYP` zamiast milczeć przy nierozpoznanym typie; `obsluz_wzor_plik` robi `shutil.rmtree(katalog_wzoru, ignore_errors=True)` w obu gałęziach except (limit rozmiaru i inny wyjątek), więc nieudane pobranie nie zostawia pustego katalogu; `/status` liczy `sum(1 for katalog in ... if (katalog / "wzor.json").is_file())` zamiast `len(list(iterdir()))`.
- `tests/pomocnicze.py`: `SesjaTestowa.make_request` dostał gałąź `SendDocument` (zwraca `Message` z `document` i `caption`, tak jak `SendMessage` już miał) i nowy `dokument_za_duzy` obok istniejącego `plik_za_duzy`, żeby testy mogły osobno symulować `TelegramEntityTooLarge` na wysyłce, niezależnie od pobierania.
- **Pułapka przy edycji przez duże bloki tekstu.** Przy wklejaniu nowych testów na koniec `test_drugi_wzor_dostaje_pozycje_w_kolejce` `old_string` w jednym z wywołań `Edit` nie objął całego ciała testu (brakło końcowych trzech linii: `zwolnij.set()`, `czekaj_na_kolejke`, asercji), więc te trzy linie zostały odcięte od swojej funkcji i osierocone na końcu pliku, poza jakąkolwiek funkcją (`NameError: name 'zwolnij' is not defined` przy zbieraniu testów). Złapane od razu przez pełne uruchomienie testów po dopisaniu, poprawione przeniesieniem linii z powrotem na właściwe miejsce. Wniosek do siebie: przy dopisywaniu kodu za pomocą `Edit` na końcu istniejącej funkcji zawsze sprawdzać, gdzie faktycznie kończy się ta funkcja, nie zakładać na podstawie samej treści ostatniej widzianej asercji.
- **Czas testów blisko progu 60 s.** Po zadaniu 3.4 cały zestaw szedł 55 do 59 s (bywało tuż nad progiem przy pomiarze `time`, licząc narzut startu Pythona poza samym `pytest -q`). Zamiast dorzucać nowe testy bota bez zmian, przed ich dopisaniem skrócono najdroższe testy renderu z zadania 3.4: wzór syntetyczny z 8 na 4 cięcia (`wzor_syntetyczny_4_ciecia`, było `wzor_syntetyczny_8_ciec`) i klik z 10 s na 6 s we wszystkich testach `renderuj`/CLI w `tests/test_render.py` i w nowym `przygotuj_wzor_i_utwor` w `tests/test_bot.py`. Po zmianie i po dopisaniu testów zadania 3.5 cały zestaw 114 testów w 55,2 s. Najdroższy pojedynczy test to `test_cli_sukces_i_blad` (7,5 s, prawdziwy podproces Pythona z zimnym importem), nietknięty, bo skraca się już tylko kosztem realizmu testu CLI. Margines do progu 60 s jest nadal wąski: zadanie 3.6 dopisze pomiar jako osobny skrypt (nie wchodzi do `pytest`), więc nie powinno tego pogorszyć, ale kolejne zmiany w częściach 4 do 6 dodadzą własne testy do tego samego zestawu i mogą go przekroczyć.
- Testy: 15 nowych w `tests/test_bot.py` (sukces z jednym `SendDocument`, błąd renderu ze stanem `blad`, brak wzoru i brak utworu nie dodają zadania, `--limit-mb` zależny od `telegram_api_url`, `TelegramEntityTooLarge` przy wysyłce, PDF w trakcie zbierania, nieudane pobranie wzoru nie zostawia katalogu, `/status` pomija katalog bez `wzor.json`) plus poprawka istniejącego `test_gotowe_czeka_na_trwajace_pobrania` (dostał wzór i utwór na dysku, bo teraz wymagane), razem 114 z 114 w 55,2 s. Commit `bot: montaż po /gotowe`.

### 2026-09-23 (część 3, zadanie 3.4: montaż, dźwięk, przebieg końcowy, CLI)
- `src/render.py`: `render.renderuj(wzor_json, katalog_projektu, utwor, wyjscie, szerokosc=1080, wysokosc=1920, fps=30, limit_mb=50) -> dict` i CLI (`argparse`, `python src/render.py --wzor --projekt --utwor --wyjscie [--szerokosc --wysokosc --fps --limit-mb]`, kod 0 lub błąd jedną linią na stderr). Przebieg: `magazyn.lista_materialow` → `czas_trwania` (ffprobe) dla klipów, brak strumienia od razu do `materialy_pominiete` → `przygotuj_materialy` (zadanie 3.3) → jedno „ślepe” wywołanie `plan_ujec` z materiałem zastępczym tylko po to, żeby policzyć medianę długości ujęć wzoru (ogranicza do 0,5 do 2,0 s) → `wstawki` → właściwe `plan_ujec` → `segment_zdjecia`/`segment_klipu` na każde ujęcie → `sklej_segmenty` (concat demuxer, bez ponownego kodowania, te same parametry ze wszystkich segmentów z zadania 3.3) → `przebieg_koncowy` (jedyne miejsce z ustawieniami kodowania pliku wynikowego: audio z utworu przez `atrim`+`afade` scalone w tym samym przebiegu co obraz filtrem `-filter_complex`, CRF 20, H.264 High, AAC 48 kHz stereo 192 kb/s, `+faststart`) → `zweryfikuj_wynik` (wymiary, fps, długość do 1 klatki, dźwięk, rozmiar) → podsumowanie JSON obok wyniku, `praca/` kasowana dopiero po sukcesie.
- **Odkrycie: `-maxrate`/`-bufsize` same nie wystarczają na krótkich klipach.** Pierwsza wersja (`bufsize = 2 × maxrate`, domyślne wypełnienie bufora VBV) dawała na 3,7 s czystego szumu (najgorszy przypadek nieściśliwości) plik 2,48 MB przy limicie 2 MB: libx264 domyślnie startuje z buforem wypełnionym w około 90%, więc na krótkim materiale zdąży „spalić” prawie cały ten zapas zanim ograniczenie realnie zadziała. Naprawa (dwie zmiany razem): `bufsize = maxrate` (zamiast `2×`) i `-x264-params vbv-init=0` (bufor startuje pusty, limit działa od pierwszej klatki). Po zmianie ten sam szum mieści się w 1,67 MB. Sprawdzone też, że to nie psuje jakości potrzebnej do testu 2 (cięcia nadal trafiają dokładnie w granice planu).
- **Zaokrąglenie w dół zamiast błędu, gdy dummy `plan_ujec` widzi materiał zastępczy.** `plan_ujec` do policzenia mediany długości ujęć wzoru (przed właściwym `wstawki`) dostaje jeden sztuczny materiał typu `zdjecie` (żeby nie potrzebował klucza `od_s`, którego prawdziwy, jeszcze nie podzielony na wstawki materiał typu `klip` by nie miał) — złapane na pełnym przebiegu (`KeyError: 'od_s'`) i poprawione przed napisaniem testów.
- Test 2 (cięcia na swoim miejscu) porównuje granice z dokładnością do 1 klatki, nie dokładnie: `analyze.wykryj_ciecia` może dołożyć czasem klatkę odchylenia od kompresji, kontrakt zadania i tak dopuszcza dokładność „do 1 klatki”.
- Testy: 5 nowych w `tests/test_render.py` (pełny przebieg, cięcia na miejscu przez `wykryj_ciecia`, limit rozmiaru na szumie, `zrodlo` usunięte z wzoru nie przeszkadza, CLI sukces i błąd), razem 104 z 104 w 55,5 s. Zapas do progu 60 s z planu jest wąski (utwór 10 s do wykrycia tempa i podwójne kodowanie w `renderuj` to najdroższe testy); do obserwacji przy zadaniach 3.5 i 3.6, które dodadzą kolejne testy do tego samego zestawu. Commit `render: montaż, dźwięk i limit rozmiaru`.

### 2026-09-23 (część 3, zadanie 3.3: segmenty zdjęć i klipów)
- `src/magazyn.py`: `gif` dołączony do `ROZSZERZENIA_KLIP`, test `typ_pliku("a.gif", None) == "klip"` w `tests/test_magazyn.py`.
- `src/render.py` dostał: `przygotuj_zdjecie` (EXIF przez `ImageOps.exif_transpose`, HEIC przez `pillow_heif.register_heif_opener()`, przezroczystość spłaszczana na czarne tło, zmniejszenie do `2x` docelowego rozmiaru z zachowaniem proporcji jako zapas na zoom, zapis do PNG w `praca/`), `segment_zdjecia` (`zoompan` liczony na roboczej rozdzielczości `2x` i dopiero potem skalowany w dół filtrem lanczos, żeby nie drgał; kierunek zoomu z parzystości numeru ujęcia), `segment_klipu` (`scale...increase,crop,fps,tpad=stop_mode=clone:stop=-1,setsar=1`; `tpad` z `stop=-1` razem z `-frames:v` daje jednym filtrem dokładnie to, co opisuje kontrakt: segment gra plik źródłowy dalej, niezależnie od długości kawałka z `wstawki`, i zamraża dopiero gdy skończą się realne klatki źródła), `przygotuj_materialy` (próba PIL dla zdjęć i `ffprobe -select_streams v:0` dla klipów, błędy trafiają do `materialy_pominiete` z polem `powod`, m.in. stały tekst „brak strumienia wideo”).
- Segmenty (zdjęcia i klipy) kodowane tymi samymi parametrami (`libx264 veryfast crf 18 yuv420p`), żeby dało się je skleić bez ponownego kodowania w zadaniu 3.4.
- Potwierdzone eksperymentalnie przed napisaniem testów (ten sam plik testowy z `wideo_z_cieciami`, kolory sekund 3 i 4): kawałek zaczynający się w 3,0 s na ujęciu 1,5 s faktycznie gra dalej przez granicę ujęcia wzorca w pliku źródłowym (kolor sekundy 3 na starcie, kolor sekundy 4 na końcu), a kawałek od 4,5 s na ujęciu 1,0 s ma 30 klatek mimo że plik ma tylko 0,5 s materiału od tego miejsca (`tpad` zamraża resztę).
- Testy: 7 nowych w `tests/test_render.py` (obrót 90 w segmencie klipu, EXIF 6 w segmencie zdjęcia, PNG z przezroczystością, krótki klip dopełniony do dokładnej liczby klatek, zdjęcie poziome daje docelowy rozmiar i SAR 1:1, uszkodzony plik w `materialy_pominiete` obok dobrego, gra dalej i zamraża dopiero na końcu pliku) plus 1 w `test_magazyn.py`, razem 99 z 99. Commit `render: segmenty zdjęć i klipów`.

### 2026-09-23 (część 3, zadanie 3.2: plan ujęć)
- `src/render.py`: `plan_ujec` i `wstawki` według kontraktów z planu, obie czyste funkcje bez ffmpeg. `znajdz_start_uderzenia` (szukanie najmniejszego całkowitego `s`) jako osobna funkcja pomocnicza, używana też w teście do niezależnego przeliczenia oczekiwanych granic klatek (test „dryf”).
- **Decyzja własna: co dokładnie wyzwala `ValueError` przy „utwór za krótki na całe okno”.** Plan nie podał liczb dla tego przypadku (w przeciwieństwie do pozostałych 10 testów, które mają dokładne wartości). Przyjąłem najprostszą, jednoznaczną regułę: `liczba_klatek <= 0` po policzeniu granic. Przy zwykłych, rosnących `ciecia_uderzenia`/`koniec_uderzenia` i dodatnim odstępie uderzeń funkcja `czas_z_pozycji` jest ściśle rosnąca, więc w praktyce to wyzwala się tylko przy zdegenerowanym oknie wzoru (np. `koniec_uderzenia == ciecia_uderzenia[0]`), nie przy „krótkim” realnym utworze z dodatnim tempem: taki zawsze da dodatnią liczbę klatek dzięki ekstrapolacji medianą odstępu. Test w kodzie używa właśnie zdegenerowanego okna. Do potwierdzenia przy odbiorze, czy to zgodne z intencją: rozważana alternatywa (żądanie końca ponad `len(uderzenia_utworu)-1`, czyli wymagająca ekstrapolacji) odrzucona, bo ekstrapolacja jest jawnie wspieranym zachowaniem `czas_z_pozycji` (rozdział kontraktu części 1/2), a nie błędem.
- Testy: 11 nowych w `tests/test_render.py` zgodnie z listą w planie (dryf na 64 cięciach, przypadek liczony ręcznie, start przy pierwszym uderzeniu blisko zera, wzór bez uderzeń, modulo materiałów, wstawki+plan_ujec razem z kawałkiem krótszym niż ujęcie, dwa cięcia w tej samej klatce, `ValueError`, trzy przypadki `wstawki` z ogonem, kolejność rundami, `start_w_klipie_s` niezależny od długości ujęcia), razem 92 z 92. Commit `render: plan ujęć w rytmie utworu`.

### 2026-09-23 (część 3, zadanie 3.1: generator zdjęć i klipów)
- Gałąź `render` utworzona od `main` (`0e84cba` jest przodkiem, `git diff --stat 0e84cba -- src/ tests/` pokazuje tylko zmiany części 7.2/7.3 w `bot.py`/`konfiguracja.py`, kotwice `magazyn.py` i `analyze.py` z planu bez przesunięć). Baseline przed zadaniem: 75 z 75.
- Dopisane do `tests/generuj.py`: `zdjecie_testowe` (obrót EXIF 6/8 przez odwrotną transpozycję PIL względem tej, której użyje `ImageOps.exif_transpose`, przezroczysty róg dla `alfa=True`), `klip_testowy`, `szum`. `requirements.txt` dostał `pillow` i `pillow-heif` (część 3 ich potrzebuje, nie doinstalowane wcześniej).
- **Odkrycie o `-display_rotation` w ffmpeg 8.1.** To opcja wejścia, nie wyjścia (potwierdzone błędem ffmpeg i dokumentacją): podana wprost przy kodowaniu z pipe'a rawvideo powoduje wypieczenie obrotu w pikselach (autorotate podczas filtrowania), a nie zapis metadanych. Żeby dostać plik z samą metadaną obrotu (tak jak prawdziwe wideo z telefonu), potrzebne są dwa przebiegi: najpierw zwykłe kodowanie bez obrotu, potem remux `ffmpeg -display_rotation <kąt> -i posredni.mp4 -c copy wynik.mp4` (bez ponownego kodowania metadane trafiają do `side_data_list` jako `Display Matrix`, zgodnie z tym, co już czyta `analyze.obrocony_o_90`). Stary tag `-metadata:s:v:0 rotate=90` nie działa w tej wersji ffmpeg (sprawdzone empirycznie, zero efektu). `klip_testowy` używa tego dwuprzebiegowego remuxu przy `obrot != 0`.
- Kierunek obrotu potwierdzony eksperymentalnie (ffmpeg opisuje `-display_rotation` jako kąt przeciwny do ruchu wskazówek zegara, jak stosowany do surowej klatki daje wynik wyświetlania): żeby po autorotacji wyjść na czerwień u góry, surowa klatka dla `obrot=90` to `np.rot90(wyswietlana, k=-1)`, dla `obrot=270` to `k=1`. Sprawdzone ręcznie w scratchpadzie przed wpisaniem do kodu, potem powtórzone jako test w `tests/test_render.py`.
- Testy: 6 nowych w `tests/test_render.py` (exif_transpose dla orientacji 1/6/8, klip bez obrotu, klip z obrotem 90 i 270, PNG z przezroczystością, szum), razem 81 z 81. Commit `testy: zdjęcia i klipy z obrotem`.

### 2026-09-23 (punkt g: prawdziwy wzór 4K na serwerze)
- Właściciel wysłał wzór do `@cwel54_bot`, bot go przyjął i przeanalizował. Liczby z serwera: `zrodlo.mp4` 160 132 475 B (153 MiB) pobrany w 8,98 s (czas obsługi update), `wzor.json` zapisany 51 s później, czyli od wysłania do podsumowania około 60 s. Próg 150 s z mapy nie został przekroczony, więc zadanie 5.0 (lekka kopia wzoru) odpada i idziemy prosto w część 3.
- Wynik analizy na prawdziwym pliku zgodny z pomiarami lokalnymi: 2160x3840, 60 fps, 31,95 s, tempo 164,1 BPM, 33 wartości w `ciecia_s`.
- Szczyt pamięci kontenera bota (`/sys/fs/cgroup/memory.peak`) to 937 455 616 B, czyli 894 MiB przy limicie 3 GB. Zapas duży, limit zostaje.
- Wolumen serwera Bot API po pobraniu ma 40 KB, więc usuwanie źródła z zadania 7.3 działa na produkcji: plik 153 MB nie leży na dysku drugi raz.
- Właściciel potwierdził, że reguły 1 do 5 znikają z `CLAUDE.md` celowo (commit `f0780fc`). Zasady stylu, pomiarów i commitów zostają w blokach WSPÓLNE planów i stamtąd obowiązują wykonawców.

### 2026-09-23 (przełączenie serwera na głównego bota, punkty a do f i h)
- Wykonane przeze mnie (Opus) na prośbę właściciela, na serwerze `46.62.151.181`: wylogowanie bota testowego z serwera lokalnego (`{"ok":true}`), `docker compose down` i usunięcie wolumenu `edity-bot_bot_api_dane`, `.env` głównego bota na serwer z `TELEGRAM_API_URL=http://bot-api:8081`, `logOut` głównego bota na zwykłym API (`{"ok":true}`), wdrożenie przez `wdroz.ps1`, weryfikacja, cron sprzątający.
- **Błąd w obrazie `bot-api` złapany przy pierwszym budowaniu.** `deluser` w busyboksie kasuje też grupę o tej samej nazwie, więc `delgroup telegram-bot-api` przerywał build komunikatem „unknown group”. Poprawka w `bot-api/Dockerfile` (commit `1611573`): kasowanie użytkownika i grupy jest tolerancyjne (`2>/dev/null` i `;`, działa niezależnie od tego, czy grupa została), tworzenie i `chown` dalej twarde (`&&`). Drugie wdrożenie (`-Wymus`, bo poprawka nie była jeszcze na GitHubie) zbudowało oba obrazy.
- Stan po wdrożeniu: `@cwel54_bot` poluje przez serwer lokalny, `bot-api` działa jako UID i GID 1000, katalog roboczy i tymczasowy należą do 1000, kontener `bot` czyta katalog bota bez błędu (blokada z odbioru zniknęła), `docker inspect` pokazuje `mem=3221225472`, `cpus=2`, użytkownik `bot`.
- **Końce linii.** `.env` skopiowany z Windows miał CRLF we wszystkich 8 liniach. Poprawione na serwerze (`sed -i "s/\r$//"`) i w źródle `C:\Dev\edity-bot-serwer.env`. Przy każdej kolejnej kopii `.env` sprawdzać, bo token z `\r` na końcu dałby 401 przy starcie.
- Sprzątanie: `/etc/cron.d/edity-bot` (4:10, projekty starsze niż 7 dni), celowo osobny plik zamiast `crontab -e`, żeby nie dotykać crona drugiego projektu na tym serwerze.
- Zostaje punkt g: właściciel wysyła prawdziwy wzór 4K do `@cwel54_bot`, mierzymy czas od wysłania do podsumowania i szczyt pamięci (`docker stats`). Ponad około 150 s oznacza zadanie 5.0 przed częścią 3.

### 2026-09-23 (odbiór zadania 7.3, Opus)
- Werdykt: OK, do scalenia. Testy 75 z 75 (38,8 s), pomiar kopiowania w progach z zapasem (maks opóźnienie pętli 14,2 ms przy progu 50 ms, kopia 1 GB 2,3 do 2,5 s), decyzja o `ROZMIAR_KAWALKA_KOPII_B` oparta na rozrzucie powtórzeń, a nie na pojedynczym przebiegu.
- Sprawdzone punkt po punkcie: `bot-api/Dockerfile` przypięty do `10.3` z UID i GID 1000 oraz `chown` na katalogu roboczym i tymczasowym; `pobierz_plik` w trybie lokalnym z limitem 1800 s, kopią w wątku i usunięciem źródła dopiero po sukcesie; pięć testów pokrywa wszystkie przypadki z planu, w tym błąd usuwania i błąd kopiowania; `wdroz.ps1` z `git fetch origin main`, porównaniem commitów, przełącznikiem `-Wymus` i `git archive main`; `mem_limit: "3g"`; `CLAUDE.md` bez „royalty free”.
- Uwaga do decyzji właściciela: commit `b573d5c` („wdrożenie: Pomiary do repozytorium”) wykracza poza zadanie 7.3. Dołożył cały katalog `Pomiary/` do repozytorium, usunął go z `.gitignore` i zmienił regułę 9 w `CLAUDE.md`. Repo jest prywatne, a w plikach nie ma tokenu, `api_id` ani hasha (sprawdzone `git grep` po wzorcach), więc nic nie wyciekło. Skutki uboczne: `wdroz.ps1` wysyła teraz `Pomiary/` na serwer (do obrazu nie wchodzą, `.dockerignore` je pomija), a każda zmiana planu albo dziennika jest od teraz zmianą w gicie do scommitowania. Zostawić albo cofnąć ten jeden commit przed scaleniem.
- Niepotwierdzone lokalnie, bo brak Dockera: budowa obrazu `bot-api` i działanie `mem_limit`. Do sprawdzenia w punkcie f (`docker inspect` ma pokazać 3221225472 bajty i UID 1000).
- `README.md` linia 3 nadal mówi o muzyce royalty free (poza zakresem zadania, wychwycone przez wykonawcę): do poprawki przy najbliższym commicie.

### 2026-09-23 (zadanie 7.3, poprawki po odbiorze)
- Wykonane na gałęzi `wdrozenie-poprawki` od `origin/main` (lokalny `main` był już zgodny z `origin/main`, bez potrzeby synchronizacji). Cztery commity zgodne z listą planu.
- **Uprawnienia serwera Bot API.** Sprawdzone przez GitHub (`gh api repos/aiogram/telegram-bot-api`, plik `Dockerfile` i `docker-entrypoint.sh` z gałęzi `master`), bo lokalnie nie ma Dockera: obraz bazowy jest na Alpine, użytkownik `telegram-bot-api` ma UID i GID 101 (`addgroup`/`adduser` busybox), entrypoint przy starcie robi `chown` tylko na katalogu z `TELEGRAM_WORK_DIR`, katalogu `TELEGRAM_TEMP_DIR` (`/tmp/telegram-bot-api`) nie rusza, zgodnie z opisem w planie. Nowy `bot-api/Dockerfile` na bazie `aiogram/telegram-bot-api:10.3`: usuwa użytkownika i grupę 101 (`deluser`, `delgroup`, oba to busybox, dostępne bez dodatkowych pakietów) i odtwarza je z UID i GID 1000, potem `chown -R` na obu katalogach (`/var/lib/telegram-bot-api` i `/tmp/telegram-bot-api`), żeby katalog tymczasowy też należał do 1000 od startu, niezależnie od entrypointu. `docker-compose.yml`: usługa `bot-api` dostała `build: ./bot-api` zamiast `image: aiogram/telegram-bot-api:latest`. Niepotwierdzone realnym budowaniem obrazu (brak Dockera lokalnie), tylko czytaniem źródeł obrazu bazowego.
- **`pobierz_plik` w trybie lokalnym.** Rozbite na `pobierz_plik` (bez zmian w trybie zdalnym, wciąż `bot.download`) i nową `pobierz_plik_lokalnie` używaną, gdy `bot.session.api.is_local`: woła `bot.get_file(file_id, request_timeout=LIMIT_GETFILE_LOKALNY_S)` (nowa stała, 1800 s zamiast domyślnych 60 s sesji aiogram), kopiuje plik spod `file_path` nową funkcją `kopiuj_plik` (w wątku przez `asyncio.to_thread`, żeby nie blokować pętli zdarzeń), potem usuwa plik źródłowy; nieudane usunięcie (`OSError`, np. `PermissionError`) tylko loguje ostrzeżenie i nie przerywa pobrania. Błąd kopiowania (np. `OSError`) zostawia źródło nietknięte, bo usuwanie jest dopiero po udanej kopii. Pięć nowych albo rozszerzonych testów w `tests/test_bot.py` (istniejący test kopiowania rozszerzony o sprawdzenie zniknięcia źródła, plus cztery nowe: limit czasu `get_file`, brak usuwania na zwykłym API, tolerancja błędu usuwania, zachowanie źródła przy błędzie kopiowania). Testy: 75 z 75 (było 71).
- **Pomiar `Pomiary/measure_pobieranie.py`.** `kopiuj_plik` na plikach 100 MB i 1 GB w katalogu tymczasowym (`tempfile.TemporaryDirectory`, sprzątane automatycznie), naprzemiennie 64 KB i 1 MB po 3 razy, z zadaniem znacznikowym co 10 ms mierzącym opóźnienie pętli zdarzeń w trakcie kopii wykonywanej w wątku. Wynik: sekcja A (100 MB) średnio 0,223 s (64 KB) i 0,224 s (1 MB), maks opóźnienie pętli 14,0 ms w obu; sekcja B (1 GB) średnio 2,474 s (64 KB) i 2,320 s (1 MB), maks opóźnienie 9,3 i 14,2 ms. Obie sekcje PASS (próg 50 ms, spory zapas: `asyncio.to_thread` skutecznie odsuwa kopiowanie od pętli zdarzeń). Różnica między rozmiarami kawałka (0,001 s i 0,154 s) nie przekracza rozrzutu powtórzeń (0,050 s i 0,563 s) w żadnej sekcji, więc kodu nie zmienia: `ROZMIAR_KAWALKA_KOPII_B = 64 * 1024`, czyli tyle, ile domyślnie używa aiogram, zgodnie z zasadą „większy kawałek wchodzi do kodu tylko wtedy, gdy różnica przekracza rozrzut”. Wyniki zapisane też do `outputs/measure_pobieranie.json`.
- **`wdroz.ps1`.** Dostał `git fetch origin main` (wyłącznie do porównania, kod nadal nie jedzie przez GitHuba) i porównanie `git rev-parse main` z `git rev-parse origin/main`: przy różnicy w dowolną stronę skrypt zatrzymuje się z komunikatem o synchronizacji, chyba że podano nowy przełącznik `-Wymus`. Pakowanie zmienione z `git archive HEAD` na `git archive main`, nazwa archiwum też zmieniona (`edity-bot-main.tar`), żeby nie sugerować, że to wciąż `HEAD`. Sekcja „Wdrożenie” w `README.md` poprawiona pod nowe zachowanie. Niepotwierdzone na serwerze (krok e właściciela).
- **`mem_limit` bota.** `docker-compose.yml`: usługa `bot` dostała `mem_limit: "3g"` (ta sama składnia co istniejące `cpus: "2"`, bez `deploy.resources`, bo to zwykły `docker compose`, nie swarm), zgodnie z decyzją właściciela z 2026-09-22. Do korekty po pomiarze zużycia pamięci w punkcie g.
- **`CLAUDE.md`.** Linia 3 poprawiona z „z muzyką royalty free” na „z muzyką z biblioteki właściciela”, zgodnie z decyzją 6 w mapie planów (`licencje.csv` przestało być bramką 2026-09-22). Ta sama nieaktualna fraza jest też w `README.md` (linia 3), ale plan zadania 7.3 wymieniał tylko `CLAUDE.md`, więc README zostawione bez zmian: do poprawki przy najbliższej okazji.
- Nieoczekiwane różnice w stosunku do opisu w planie: brak, plan zgadzał się z kodem z `e9a4755` (numery linii i nazwy funkcji bez przesunięć).

### 2026-09-23 (biblioteka muzyki i próbki)
- Tempa plików z `dane/muzyka/` zmierzone przez `analizuj_rytm` (`START_BPM = 150`): `Hot N Cold (Hardstyle).mp3` 164,1 BPM (279 uderzeń od 1,46 s do 102,56 s, mediana odstępu 364 ms, dalej tracker nie znajduje uderzeń przy długości 151,8 s), `Ex's And Oh's (Hardstyle).mp3` 175,2 BPM (442 uderzenia, 154,9 s).
- `MR.mp4` ma dźwięk (AAC 48 kHz) i dekoduje się bez błędu, ale trwa 10,9 s i `beat_track` nie znajduje w nim ani jednego uderzenia, więc jako utwór odpada. Zasada z planu 4 (kandydat musi mieć znane tempo i mieścić okno wzoru na siatce uderzeń) to wyłapuje, indeksowanie ma go pominąć z wpisem w logu.
- `dane/muzyka/staly.mp3` to kopia `Hot N Cold (Hardstyle).mp3`: jej tempo zgadza się z wzorem `0915` (oba 164,1 BPM), więc cięcia w części 3 będą padać na uderzenia. Na serwer trafi przed testem części 3.
- Próbki przeniesione: `0915.mp4` i `0921.mp4` z `dane/wzory/Edity/` do `dane/probki/wzory/`, katalog `Edity` usunięty, więc `/status` nie liczy już fałszywego wzoru.
- Do części 4 brakuje różnorodności: dwa utwory leżą blisko siebie (164 i 175 BPM), więc dobór po tempie nie ma z czego wybierać.

### 2026-09-22 (decyzje po odbiorze części 7)
- Właściciel: przy wdrożeniu 7.3 serwer przechodzi na głównego bota (punkty a do h w planie 7), a bot testowy wraca do pracy lokalnej na zwykłym API. `mem_limit` bota 3 GB zatwierdzony.

### 2026-09-22 (odbiór części 7, Opus)
- Werdykt: poprawki, zadanie 7.3 w `PLAN_EDITY_7_WDROZENIE.md`. Kod 7.1 i 7.2 zgodny z planem poza `wdroz.ps1`; testy 71 z 71 lokalnie (28 s). Serwer sprawdzony tylko odczytem: obie usługi działają, bot jako UID 1000, limit CPU 2, logi rotowane, brak wystawionych portów, `TELEGRAM_API_URL=http://bot-api:8081` w `.env` serwera, Docker włączony przy starcie systemu.
- Blokujące: serwer Bot API działa jako UID 101 i zakłada katalog bota z prawami `drwxr-x---` (pliki `-rw-------`), a bot w kontenerze dostaje `Permission denied` już na katalogu. Każde pobranie przez serwer lokalny skończy się komunikatem „nie udało się pobrać”. Nie wyszło to wcześniej, bo nikt jeszcze nie wysłał pliku (na serwerze puste `dane/wzory` i `dane/projekty`), a test jednostkowy używa pliku tego samego użytkownika.
- Pozostałe: `wdroz.ps1` pakuje `HEAD` i nie porównuje `main` z `origin/main` (zadanie 7.1 poprawione w planie już po commicie `4e01bb9`); `getFile` w trybie lokalnym ma limit 60 s (domyślny aiogram), a serwer odpowiada dopiero po ściągnięciu całego pliku; pobrany plik leży dwa razy (wolumen serwera Bot API i `dane/`); brak `mem_limit` na serwerze współdzielonym (4 vCPU, 7,6 GB RAM, API drugiego projektu około 1,8 GB, 31 GB wolnego dysku); obraz `aiogram/telegram-bot-api:latest` bez przypiętej wersji.
- Na serwerze działa bot testowy, bo trafił tam lokalny `.env`; `C:\Dev\edity-bot-serwer.env` (główny bot) czeka nieużyty. Zalecenie: przełączenie na głównego bota przy wdrożeniu 7.3 (punkty a do e w planie 7).
- Zmiany produktowe sprawdzone: decyzja 6 (muzyka bez warunku licencji) spójna w mapie i planie 4. Dopisane przy odbiorze: wspólny blok planów i plan 3 nie mówią już o muzyce royalty free, `staly.mp3` to dowolny utwór z biblioteki pod tą nazwą, plan 4 przyjmuje mp4 (dźwięk z wideo, bo w bibliotece leży `MR.mp4`). Opis w `CLAUDE.md` poprawi zadanie 7.3.
- Restart VPS nietestowany: potwierdzony konfiguracją, prawdziwy reboot tylko w oknie serwisowym, bo zatrzymuje też drugi projekt.

### 2026-09-22 (decyzja: licencje.csv przestaje być bramką)
- Właściciel dodał do `dane/muzyka/` dwa remiksy hardstyle znanych, objętych prawem autorskim piosenek („Hot N Cold” Katy Perry, „Ex's & Oh's” Elle King), argumentując, że to muzyka z TikToka i edity wracają na TikTok. Zwróciłem uwagę, że licencja biblioteki dźwięków TikToka obowiązuje tylko w aplikacji TikTok, a to nie neutralne utwory royalty free tylko pełnoprawne piosenki w wersji bootleg. Po wyjaśnieniu, że docelowa platforma to sam TikTok (czyli sposób użycia zgodny z tym, do czego biblioteka dźwięków TikToka jest pomyślana), właściciel podtrzymał decyzję.
- **Decyzja właściciela (2026-09-22):** `licencje.csv` przestaje być warunkiem doboru utworu w części 4. Każdy plik z `dane/muzyka/` jest kandydatem niezależnie od wpisu w CSV; wpis w CSV daje tylko dane do podpisu pod postem (tytuł, autor, licencja), gdy jest. Zaktualizowane: `PLAN_EDITY_0_MAPA.md` (decyzja 6, tabela części, sekcja „Muzyka: skąd brać”) i `PLAN_EDITY_4_MUZYKA.md` (Efekt, Od właściciela, kontrakt `music.wybierz_utwor` — pole `licencja` może być `null`, `ValueError` już nie liczy braku licencji jako powodu odrzucenia, zadanie 4.3 — podpis pomija linię o muzyce, gdy brak wpisu). Część 4 nadal nie ruszona (nie ma kodu), więc to czysto zmiana planu.

### 2026-09-22 (wdrożenie na VPS, ciąg dalszy)
- Właściciel dodał pliki muzyki (`Ex's And Oh's (Hardstyle).mp3`, `Hot N Cold (Hardstyle).mp3`, `MR.mp4` w `dane/muzyka/`, bez `licencje.csv` na razie). Skopiowane na serwer (`scp -r dane/muzyka root@46.62.151.181:/opt/edity-bot/dane/`), właściciel ustawiony na UID 1000. `licencje.csv` dołożyć i skopiować ponownie, gdy powstanie.
- Właściciel potwierdził: limit wysyłki z lokalnym serwerem Bot API to **2000 MB** (nie 200). Poprawione w `PLAN_EDITY_7_WDROZENIE.md` (zadanie 7.2, sekcja Constraints), zgodne z tym, co już było w kodzie (`LIMIT_WYSYLKI_TELEGRAM_LOKALNY_MB = 2000` w `bot.py`) i w testach.

### 2026-09-22 (wdrożenie na VPS)
- Pierwsze realne wdrożenie na serwer `46.62.151.181` (Ubuntu 24.04, Docker 29.6.2, Compose v5.3.1, już zainstalowane). Na serwerze działa też osobny, niezwiązany projekt (`docker-frontend-next`, `docker-api`, `docker-caddy` za Caddy na portach 80 i 443): sprawdzone przed jakąkolwiek zmianą (kontenery, `/opt`, porty), nie tknięte, brak kolizji (edity-bot nie wystawia żadnego portu, katalog `/opt/edity-bot` osobny).
- Kopiowanie `.env` na serwer przez `scp` zablokował klasyfikator trybu auto (plik z prawdziwym tokenem): wykonał to właściciel ręcznie.
- Rozbieżność z wpisem z wcześniej tego dnia: lokalny `.env` NIE miał `TELEGRAM_API_ID` ani `TELEGRAM_API_HASH` mimo notatki, że „już są”. Właściciel dopisał je w trakcie sesji.
- **Błąd znaleziony i naprawiony:** `Dockerfile` z zadania 7.1 nie kopiował `pytest.ini` do obrazu (plan z zadania 7.1 wymieniał tylko `src/`, `tests/`, `zasoby/`, `requirements*.txt`, ale nie plik konfiguracji pytest z korzenia repo). Efekt: w kontenerze `asyncio_mode = auto` nie działało, siedem testów kończyło się `ERROR` (brak obsługi asynchronicznych fixture'ów) i siedem `FAIL`. Poprawka: `COPY pytest.ini ./` w `Dockerfile`, commit `wdrożenie: dokopiuj pytest.ini do obrazu` (`0f924ea`). Po poprawce w kontenerze 71 z 71.
- Przebieg: `docker compose up -d --build` zbudował obraz (agent apt, ~50 s od zera z `libgl1`/`libglib2.0-0`/`ffmpeg`, `pip install -r requirements.txt` ~32 s, opencv-python i inne z gotowych kół, bez kompilacji), ściągnął `aiogram/telegram-bot-api:latest`. Oba kontenery wystartowały bez restartów. `logOut` na Telegramie wykonany dla tokenu bota testowego (`@local125_bot`, zgodnie z `.env`, nie głównego bota), potwierdzone `{"ok":true}`. Bot po starcie loguje poprawny polling przez lokalny serwer (`INFO:aiogram.dispatcher:Run polling for bot @local125_bot`), `bot-api` startuje z `--local` bez błędów.
- Testy w kontenerze (`docker compose run --rm bot sh -c 'pip install -q -r requirements-test.txt && python -m pytest -q'`): 71 z 71, jedno łagodne ostrzeżenie (`PytestCacheWarning: Permission denied` przy zapisie `.pytest_cache` w `/app`, bo katalog należy do roota a proces działa jako UID 1000; nie wpływa na wynik testów, do rozważenia przy okazji, nieblokujące).
- Nie zrobione w tej sesji (celowo, ryzyko dla współdzielonego serwera albo brak danych): rzeczywisty restart VPS (weryfikacja auto-startu po reboot), kopiowanie `dane/muzyka` (katalog jeszcze pusty lokalnie), próba na Telegramie z prawdziwym wzorem 4K (`/wzor`, `/status`) i pomiar czasu pobrania/analizy na serwerze. Zostają do właściciela.
- Adres serwera i sposób łączenia (klucz SSH już skonfigurowany, działa bez hasła) zapisane w pamięci projektu poza repo.

### 2026-09-22
- Zadanie 7.2 wykonane na gałęzi `wdrozenie` (`800557e`): `konfiguracja.py` dostał opcjonalne `telegram_api_url` (z `TELEGRAM_API_URL`, brak w env daje `None`). W `bot.py`: `efektywny_limit_mb` i nowe `efektywny_limit_wysylki_mb` przełączają górną granicę na 2500 i 2000 MB, gdy `telegram_api_url` jest ustawiony (bez adresu bez zmian: 20 i 50 MB); nowe `zbuduj_sesje` tworzy `AiohttpSession` z `TelegramAPIServer.from_base(url, is_local=True)` tylko gdy adres jest ustawiony, inaczej `None` (aiogram sam tworzy domyślną sesję). Odkrycie: `bot.pobierz_plik` nie wymagał żadnej zmiany, bo `aiogram.Bot.download_file` już sam sprawdza `session.api.is_local` i w trybie lokalnym czyta plik bezpośrednio z dysku (`aiofiles`) zamiast przez HTTP (sprawdzone w źródle `venv/Lib/site-packages/aiogram/client/bot.py:419`), więc `pobierz_plik` „kopiuje” plik już dziś, wystarczyło poprawnie skonfigurować sesję bota. `docker-compose.yml` dostał usługę `bot-api` (obraz `aiogram/telegram-bot-api`, aktywnie utrzymywany na Docker Hub, sprawdzone przez WebFetch 2026-09-22: aktualizacja około doby wstecz, 10M+ pobrań), zmienna `TELEGRAM_LOCAL=1`, `TELEGRAM_API_ID` i `TELEGRAM_API_HASH` z `.env` przez `env_file`, oraz wspólny wolumen nazwany `bot_api_dane` pod `/var/lib/telegram-bot-api` w obu usługach (to katalog roboczy oficjalnego obrazu). `.env.example` dostał puste `TELEGRAM_API_URL`, `TELEGRAM_API_ID`, `TELEGRAM_API_HASH`. Testy: 9 nowych (2 w `test_konfiguracja.py`, 7 w `test_bot.py`, w tym kopiowanie przez podmieniony `get_file` na prawdziwym pliku tymczasowym), razem 71 z 71.
- **Niezgodność w planie (do potwierdzenia przez właściciela).** `PLAN_EDITY_7_WDROZENIE.md` w opisie zadania 7.2 podaje raz granicę wysyłki z lokalnym serwerem jako 2000 MB (sekcja Context/Inputs, zgodnie z `TELEGRAM_BOT_API.md`), a raz jako 200 MB (sekcja Constraints, lista testów). Przyjąłem 2000 MB jako właściwą wartość (zgodna z dokumentacją Telegrama i z resztą planu), 200 uznałem za literówkę brakującego zera. `LIMIT_WYSYLKI_TELEGRAM_LOKALNY_MB = 2000` w `bot.py`, testy sprawdzają 2000.
- Zadanie 7.1 wykonane na gałęzi `wdrozenie` (`4e01bb9`): `Dockerfile` (`python:3.13-slim`, apt `ffmpeg`, `libgl1`, `libglib2.0-0`, użytkownik UID 1000, `NUMBA_CACHE_DIR=/app/dane/.cache/numba`, `zasoby/` kopiowane wzorcem `zasob[y]/`, żeby COPY nie wywalał się, gdy katalog jeszcze nie istnieje), `docker-compose.yml` (usługa `bot`, `env_file: .env`, wolumen `./dane:/app/dane`, `restart: unless-stopped`, `cpus: "2"`, logi `json-file` 10 MB razy 3, bez portów), `.dockerignore` (`dane`, `outputs`, `Pomiary`, `.env`, `venv`, `.git`, plus `__pycache__` i `.pytest_cache`), `wdroz.ps1` (parametr `-Serwer`, ostrzeżenie przy niezacommitowanych zmianach, `git archive HEAD`, `scp`, przez ssh `rm -rf src tests zasoby` i rozpakowanie całego archiwum, bo `git archive` i tak pomija katalogi z `.gitignore`, potem `docker compose up -d --build` i `logs --tail 20`), sekcja „Wdrożenie” w README. Weryfikacja przez czytanie plików (brak Dockera lokalnie), bez pytest. Do potwierdzenia dopiero po pierwszym realnym `docker compose up -d --build` na serwerze (krok 6 w planie, należy do właściciela).
- Odbiór części 1 i 2 (Opus, na polecenie właściciela). Część 1: OK; filtr właściciela na dispatcherze obejmuje `message` i `edited_message`, `parse_mode` jawnie wyłączony, w handlerach brak wywołań blokujących (tylko drobne operacje na plikach JSON), kolejka zabija drzewo procesów przez psutil i przeżywa wyjątek zadania, stdin zamknięte, komunikaty bez myślników, tokenu brak w historii gita, `.env`, `dane/`, `outputs/` i `Pomiary/` poza indeksem. Część 2: OK; `min_klatek=3`, dekodowanie ffmpegiem przed librosą, doprecyzowanie uderzeń, ekstrapolacja medianą odstępu, typy JSON jako `float`; ContentDetector potwierdzony na arkuszach (patrz „Znane problemy” 0). Drobne uwagi bez blokowania: punkty 0d i 0e w „Znanych problemach” oraz trzy braki wpisane do zadania 3.5.
- Właściciel scalił `analiza-wzoru` do `main` na GitHubie (PR #1, `4b929c9`); lokalny `main` zsynchronizowany. Właściciel potwierdził kolejność (część 7 przed 3) oraz długość i kolejność wstawek.
- Plany 3 do 7 i mapa poprawione po częściach 1 i 2 (przegląd kodu z `0e84cba`, 62 testy zaliczone). Najważniejsze zmiany: reguła o zdalnym repo zgodna z regułą 9 `CLAUDE.md` (gałąź na część, push na prośbę, wdrożenie przez `wdroz.ps1`); kotwice `plik:linia` z prawdziwego kodu; w części 3 limit wysyłki `efektywny_limit_wysylki_mb` (dziś `.env` ma 200 MB, zwykłe API przyjmie 50), wstawki układane rundami po materiałach (poprzedni przykład w teście 10 był niejednoznaczny), kawałek klipu krótszy od ujęcia gra dalej z pliku zamiast zamrażać się po `czas_s`, trzy drobne braki z „Znanych problemów”; w części 4 parametr `start_bpm` i porównanie 120 z 150 dla utworów; w części 5 nowe zadanie 5.0 (lekka kopia wzoru 4K do analizy); w części 7 apt `libgl1` i `libglib2.0-0`, zapisywalny `NUMBA_CACHE_DIR`, lokalny serwer Bot API jako zadanie obowiązkowe, osobny bot testowy do pracy lokalnej. Nowa zalecana kolejność: odbiór 1 i 2, scalenie `analiza-wzoru`, część 7, potem 3.
- Drugi wzór `0921.mp4` (4K, 60 fps, 32 s): tempo 139,7 BPM niezależnie od `start_bpm` (120 i 150), 15 cięć w obu detektorach (identyczne), mediana ujęcia 1,75 s, 53% cięć na uderzeniach lub półuderzeniach, arkusze bez przegapionych i fałszywych cięć. `0915.mp4` po zmianie: 31 cięć (content) i 32 (adaptive), 77% i 69% na uderzeniach. Decyzja wstępna: domyślny ContentDetector (na 0915 mniej fałszywych cięć w ruchu, na 0921 remis, szybszy), do potwierdzenia przez oceniającego. Pomiar A po zmianie nadal 100%.
- Na prośbę właściciela `START_BPM = 150.0` w `analizuj_rytm` (zmiana niezacommitowana w momencie wpisu).
- Pomiar B na `dane/wzory/Edity/0915.mp4` (właściciel podał folder; plik ma 153 MB, nie 2 GB: 2160x3840, 60 fps, 32 s, h264 40 Mb/s). Czas całego pomiaru 5 min 46 s. ContentDetector 31 cięć, 48 s; AdaptiveDetector 32 cięcia, 57 s. Ten sam wzór: 28 wspólnych cięć, AdaptiveDetector dokłada trzy cięcia w jednym ujęciu z szybkim ruchem (23,9, 24,3, 24,9 s), ContentDetector dokłada dwa cięcia na końcu (30,967 i 31,1 s). Wstępnie domyślny zostaje ContentDetector (mniej cięć w ruchu), ocena oparta na jednym wzorze.
- Pierwsza klatka wzoru to jednoklatkowa okładka (baner), oba detektory ją pomijają przez `min_klatek=3`. To pożądane.
- Pomiar właściciela puszczony ręcznie trwał ponad 30 min bez wyniku; przyczyny nie ustalono (na tym pliku pomiar trwa niecałe 6 min). Możliwe, że poszedł inny, większy plik.
- Koszt na tym pliku: dekodowanie pełnej rozdzielczości 36 s, lekka kopia 360p: 11 s kopia i 2 s detekcja, więc kopia opłaca się dopiero przy wielu przebiegach albo większych plikach. Wyszukiwanie klatki w 4K 0,6 s (arkusze).
- Odkrycie o tempie: patrz „Znane problemy” pkt 00.

### 2026-09-21
- Część 2 wykonana według `PLAN_EDITY_2_ANALIZA.md`, 2 commity zgodne z listą planu. Zależności doinstalowane w `venv`: scenedetect 0.7.1, librosa 1.0.0, numpy 2.5.3, soundfile 0.14.0 (przypięte w `requirements.txt`).
- Odkrycia: librosa `beat_track` z domyślnym krokiem 512 daje uderzenia spóźnione o 12 do 19 ms i tempo 117,5 zamiast 120 (błąd 2,08%, próg 2%). Poprawka: obwiednia z krokiem 128, potem doprecyzowanie każdego uderzenia w oknie ±50 ms na obwiedni z oknem 512 i krokiem 32 (mediana błędu około 1,7 ms). Test osi czasu mp3 kontra mp4 przechodzi z marginesem.
- Kontrakt: identyfikator wzoru w CLI to nazwa katalogu pliku wyjściowego (`dane/wzory/<id>/wzor.json`). Analiza w bocie: zapowiedź „Analizuję wzór” zawsze przed wynikiem (zadanie czeka na zdarzenie po wysłaniu zapowiedzi). `WZOR_ZAPISANY` usunięty, bo zastąpiony zapowiedzią.
- Pomiar `Pomiary/measure_analiza.py`: sekcja A 18 przypadków (6 temp razy 3 układy), wszystkie 100%. Sekcja B sprawdzona na jednym wideo syntetycznym (arkusze poprawne), usunięte z `outputs/`, żeby nie mylić oceniającego. Skrypt przyjmuje opcjonalnie katalog próbek jako argument.
- Zasada 5 planu („repo tylko lokalne”) pominięta zgodnie z aktualną regułą 9. Na GitHub nic nie wypchnięto (nie było prośby).
- Wymaganie o wstawkach dopisane do planu części 3 (`render.wstawki`, testy, `gif` w `magazyn`). Plan części 3 nadal w WSPÓLNYM bloku mówi „repo tylko lokalne”, nieaktualne po zmianie reguły 9, do poprawienia przy okazji.
- Część 1 wykonana według `PLAN_EDITY_1_SZKIELET.md`, 4 commity zgodne z listą planu. Testy 31, potem 33 po poprawkach. Pomiar: A max 10,3 ms (próg 50), B 20 z 20, C mediana 110,7 ms (tylko raport).
- Test ręczny właściciela: album ze zdjęć i klipów zapisał się częściowo, potem projekt z 2 zdjęciami i 1 klipem przeszedł przez `/gotowe`. Wzór się nie pobrał (limit 20 MB).
- Poprawki po teście: obsługa błędu pobierania, limit nie wyższy niż limit serwera Telegrama, dwa nowe testy regresji.
- Repo wypchnięte na GitHub na prośbę właściciela. Reguła 9 w `CLAUDE.md` (repo tylko lokalne) była nieaktualna, właściciel potwierdził, że dotyczyła innego repo, zmieniona.
- Właściciel zażądał: klipy i gify cięte na krótkie wstawki (patrz „Wymagania").

## Następne kroki

1. Zadanie 4.5 gotowe na gałęzi `pamiec` (testy 189 z 189, pomiar A i B w progach). Zostaje: PR, scalenie, `wdroz.ps1`, potem na serwerze sprawdzić `docker stats` przy montażu z nowym utworem w bibliotece (indeksu nie trzeba przeliczać). Bez tego każdy dłuższy nowy utwór w bibliotece może znów zabić montaż.
2. Zadanie 9.5 (nakładka z kryciem) na gałęzi `krycie`, osobnym PR-em. Przed nim klip flagi do `dane/nakladki/domyslna.mp4` albo przez `/nakladka`.
3. Części 5 i 6, potem 9 (9.1 do 9.4 oraz 9.6 do 9.8, napisy w rytmie). Po każdej odbiór na arkuszach i `wdroz.ps1`, a po części 5 na serwerze `docker compose exec bot python src/analyze.py --wszystkie`.
4. Właściciel: własny znak wodny przez `/znak` (PNG z przezroczystością), jeśli ma być inny niż plik z `dane/promocyjne/`.
